# logical index order：安全原理、源码与实现

> 所属专题：[工具并发、依赖与结果排序](../../../04-tools-security/07-concurrent-tools.md)  
> 导图路径：输出 → logical index order  
> 类型：结构感知截断节点；编号 10/10

## 1. 学习结论

“logical index order”是结构感知截断节点。掌握标准是能说明信任边界、校验顺序、授权依据、副作用时机和失败后的状态。

## 2. 概念与项目语境

`ConcurrentToolExecutor` 将前台工具直接执行，后台工具提交虚拟线程；结果带原始 index，收集后排序。FileLockManager 保护受影响路径。组件已注册，但 WebAgentOrchestrator 主链当前仍 for 循环逐个执行。

并发执行与逻辑顺序分离：任务完成顺序是非确定的，但写回模型/前端可按原 ToolCall index 保持稳定。依赖 A→B 时，排序结果不能弥补错误的并行执行，必须在调度前识别依赖。

## 3. 底层实现原理

先分类内容，再按语法行、diff hunk、错误尾部或树层级保留信息，最后用 Token hard limit 反复收缩并返回省略元数据。

从源码依次检查：输入是否规范化并限长；Schema 与业务校验是否分层；执行入口是否重新授权；资源身份/版本是否在锁内复核；副作用、结果和审计是否按确定顺序提交。

## 4. HippoBuddy 源码追踪

- [ConcurrentToolExecutor.java](../../../../../src/main/java/com/example/agent/tools/concurrent/ConcurrentToolExecutor.java)
- [FileLockManager.java](../../../../../src/main/java/com/example/agent/tools/concurrent/FileLockManager.java)
- [WebAgentOrchestrator.java](../../../../../src/main/java/com/example/agent/web/orchestrator/WebAgentOrchestrator.java)

```java
for (int i = 0; i < toolCalls.size(); i++) {
    if (executor != null && !executor.shouldRunInBackground())
        results.add(executeSingle(call, i, total));
    else
        backgroundTasks.add(call);
}
executeBackgroundTasks(backgroundTasks, results, total);

// backgroundTasks 内重新编号
for (int i = 0; i < toolCalls.size(); i++) {
    final int index = i;
    executor.submit(() -> executeSingle(call, index, total));
}
```

围绕“logical index order”沿模型可见定义追到真正 executor，确认任何绕过 Prompt 的直接调用仍会经过同一安全链。

## 5. 为什么这样实现

这样控制上下文成本又不破坏完成任务所需的结构证据。

安全保证必须位于副作用入口并默认拒绝；只隐藏工具、提示模型或检查原始字符串都属于可绕过的软约束。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.util.*;
import java.util.concurrent.*;

public class DeterministicToolsDemo {
    record Call(int index, String id, Callable<String> task) {}
    record Result(int index, String id, String value, Throwable error) {}

    static List<Result> execute(List<Call> calls) throws InterruptedException {
        Result[] slots = new Result[calls.size()];
        BitSet usedIndexes = new BitSet(calls.size());
        for (Call call : calls) {
            if (call.index() < 0 || call.index() >= slots.length || usedIndexes.get(call.index()))
                throw new IllegalArgumentException("invalid or duplicate index: " + call.index());
            usedIndexes.set(call.index());
        }
        try (ExecutorService executor = Executors.newVirtualThreadPerTaskExecutor()) {
            List<Future<?>> futures = new ArrayList<>();
            for (Call call : calls) {
                futures.add(executor.submit(() -> {
                    try { slots[call.index()] = new Result(call.index(), call.id(), call.task().call(), null); }
                    catch (Throwable e) { slots[call.index()] = new Result(call.index(), call.id(), null, e); }
                }));
            }
            for (Future<?> f : futures) {
                try { f.get(); } catch (ExecutionException impossible) { throw new AssertionError(impossible); }
            }
        }
        if (Arrays.stream(slots).anyMatch(Objects::isNull)) throw new IllegalStateException("missing result");
        return List.of(slots);
    }
    public static void main(String[] args) throws Exception {
        List<Result> r = execute(List.of(
                new Call(0, "slow", () -> { Thread.sleep(100); return "a"; }),
                new Call(1, "fast", () -> "b")));
        if (!r.get(0).id().equals("slow") || !r.get(1).id().equals("fast")) throw new AssertionError(r);
    }
}
```

围绕“logical index order”修改参数、版本、路径或时序，验证拒绝发生在副作用之前。

## 7. 疑难点与失败推演

- Schema 正确不代表调用被授权；
- normalize 不等于解析符号链接后的真实路径；
- 确认票据必须一次性并绑定参数与资源版本；
- cancel future 不能回滚已经发生的文件或进程副作用。

## 8. 实验与面试

1. 用 `rg "logical index order" src/main/java src/test/java` 搜索并沿执行链补全；
2. 构造绕过 Prompt 的直接 Tool 调用；
3. 注入重复确认、symlink、并发编辑、输出超限或乱序完成；
4. 按“威胁—控制点—源码—为什么—剩余风险”回答。

## 9. 延伸学习

- [OpenJDK JEP 444：Virtual Threads](https://openjdk.org/jeps/444)：理解虚拟线程解决的是吞吐，不自动提供结构化并发或数据安全。
- [Java 21 Concurrency Guide](https://docs.oracle.com/en/java/javase/21/core/concurrency.html)：补齐 Executor、Future、取消和同步基础。
- [Java Concurrency in Practice](https://jcip.net/)：深入任务执行、取消策略、安全发布与复合操作。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段四：工具执行与安全
- **完整路线周次**：第 28/42 周
- **专题内进度**：第 10/10 个节点
- **全课程进度**：第 351/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[completion order：安全原理、源码与实现](09.md)
- **下一知识点**：[vocabulary：底层原理、源码与实现](../../05-context-storage/01-token-budget/01.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

