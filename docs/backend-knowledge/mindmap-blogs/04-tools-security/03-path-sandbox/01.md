# resolve relative：安全原理、源码与实现

> 所属专题：[路径规范化、Sandbox 与符号链接](../../../04-tools-security/03-path-sandbox.md)  
> 导图路径：Canonicalization → resolve relative  
> 类型：路径解析与 Sandbox 节点；编号 1/12

## 1. 学习结论

“resolve relative”是路径解析与 Sandbox 节点。掌握标准是能说明信任边界、校验顺序、授权依据、副作用时机和失败后的状态。

## 2. 概念与项目语境

1. 拒绝 null/NUL/无效编码；
2. 选择明确 base root；
3. resolve relative；
4. absolute + normalize；
5. 对存在祖先 realPath；
6. 按 OS 规则比较 root；
7. 执行前重新检查；
8. 打开文件后若可行验证 file identity。

## 3. 底层实现原理

先做 lexical normalize，再用 realpath 解析已存在祖先和链接，验证 canonical target 位于允许 root；写入前重新检查。

从源码依次检查：输入是否规范化并限长；Schema 与业务校验是否分层；执行入口是否重新授权；资源身份/版本是否在锁内复核；副作用、结果和审计是否按确定顺序提交。

## 4. HippoBuddy 源码追踪

- [PathSecurityUtils.java](../../../../../src/main/java/com/example/agent/tools/PathSecurityUtils.java)

```java
Path path = Paths.get(filePath);
if (!path.isAbsolute()) path = getEffectiveRoot().resolve(path);
path = path.normalize();
if (!isWithinAllowedPath(path)) throw new ToolExecutionException(...);
if (isRelaxedMode()) return path;

Path normalizedPath = path.toAbsolutePath().normalize();
if (normalizedPath.startsWith(PROJECT_ROOT)) return true;
```

围绕“resolve relative”沿模型可见定义追到真正 executor，确认任何绕过 Prompt 的直接调用仍会经过同一安全链。

## 5. 为什么这样实现

这样防止 ..、符号链接、junction、大小写和检查—使用竞态绕过边界。

安全保证必须位于副作用入口并默认拒绝；只隐藏工具、提示模型或检查原始字符串都属于可绕过的软约束。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.io.IOException;
import java.nio.file.*;

public final class SafePaths {
    private final Path rootReal;
    public SafePaths(Path root) throws IOException { rootReal = root.toRealPath(); }

    Path existing(String userPath) throws IOException {
        Path lexical = rootReal.resolve(userPath).normalize();
        if (!lexical.startsWith(rootReal)) throw new SecurityException("lexical escape");
        Path real = lexical.toRealPath();
        if (!real.startsWith(rootReal)) throw new SecurityException("symlink escape");
        return real;
    }
    Path forCreate(String userPath) throws IOException {
        Path target = rootReal.resolve(userPath).normalize();
        if (!target.startsWith(rootReal)) throw new SecurityException("lexical escape");
        Path ancestor = target.getParent();
        while (ancestor != null && !Files.exists(ancestor, LinkOption.NOFOLLOW_LINKS))
            ancestor = ancestor.getParent();
        if (ancestor == null) throw new SecurityException("no existing ancestor");
        Path ancestorReal = ancestor.toRealPath();
        if (!ancestorReal.startsWith(rootReal)) throw new SecurityException("parent symlink escape");
        return target;
    }
    public static void main(String[] args) throws Exception {
        Path temp = Files.createTempDirectory("safe-root-");
        SafePaths safe = new SafePaths(temp);
        if (!safe.forCreate("a/b.txt").startsWith(temp.toRealPath())) throw new AssertionError();
        try { safe.forCreate("../secret"); throw new AssertionError(); }
        catch (SecurityException expected) {}
    }
}
```

围绕“resolve relative”修改参数、版本、路径或时序，验证拒绝发生在副作用之前。

## 7. 疑难点与失败推演

- Schema 正确不代表调用被授权；
- normalize 不等于解析符号链接后的真实路径；
- 确认票据必须一次性并绑定参数与资源版本；
- cancel future 不能回滚已经发生的文件或进程副作用。

## 8. 实验与面试

1. 用 `rg "resolve relative" src/main/java src/test/java` 搜索并沿执行链补全；
2. 构造绕过 Prompt 的直接 Tool 调用；
3. 注入重复确认、symlink、并发编辑、输出超限或乱序完成；
4. 按“威胁—控制点—源码—为什么—剩余风险”回答。

## 9. 延伸学习

- [OWASP Path Traversal](https://owasp.org/www-community/attacks/Path_Traversal)：系统学习编码绕过、规范化和根目录约束。
- [Oracle Java Secure Coding Guidelines](https://www.oracle.com/java/technologies/javase/seccodeguide.html)：重点看输入验证、权限最小化与文件系统安全原则。
- [Java Path API](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/nio/file/Path.html)：精读 `normalize`、`toRealPath`、`startsWith` 的精确定义。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段四：工具执行与安全
- **完整路线周次**：第 24/42 周
- **专题内进度**：第 1/12 个节点
- **全课程进度**：第 290/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[defense in depth：安全原理、源码与实现](../02-blocker-capability/12.md)
- **下一知识点**：[absolute：安全原理、源码与实现](02.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

