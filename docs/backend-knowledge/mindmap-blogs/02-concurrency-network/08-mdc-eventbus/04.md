# clear：底层原理、源码与实现

> 所属专题：[MDC 上下文传播与 EventBus](../../../02-concurrency-network/08-mdc-eventbus.md)  
> 导图路径：MDC → clear  
> 类型：上下文传播或事件分发节点；编号 4/12

## 1. 学习结论

“clear”是上下文传播或事件分发节点。掌握标准是能从线程/状态/协议层说明正常路径、竞争路径、超时路径和关闭路径。

## 2. 概念与项目语境

SLF4J MDC 通常基于 ThreadLocal 保存键值。线程池任务换线程后不会自动携带；平台线程复用时旧值又可能残留。因此提交任务时 snapshot，执行开始 restore，finally clear。

- [ ] 能解释 MDC 在线程池中的污染；
- [ ] 能实现 snapshot/restore/clear；
- [ ] 能说明同步 EventBus 的语义；
- [ ] 能区分 EventBus 与消息队列。

## 3. 底层实现原理

异步提交时复制 ThreadLocal/MDC 快照，任务 finally 恢复；EventBus 定义同步/异步、异常隔离和交付保证。

分析任何并发实现都要写出：共享状态由谁拥有；哪些操作必须原子；可见性由锁、volatile、原子类还是队列保证；容量耗尽如何传播；取消后谁负责释放实际资源。

## 4. HippoBuddy 源码追踪

- [LoggingContext.java](../../../../../src/main/java/com/example/agent/core/logging/LoggingContext.java)
- [ThreadPools.java](../../../../../src/main/java/com/example/agent/core/concurrency/ThreadPools.java)
- [EventBus.java](../../../../../src/main/java/com/example/agent/core/event/EventBus.java)

```java
public static Runnable wrapWithMdc(Runnable runnable) {
    Map<String, String> snapshot = LoggingContext.snapshot();
    return () -> {
        LoggingContext.restore(snapshot);
        try {
            runnable.run();
        } finally {
            MDC.clear();
        }
    };
}
```

围绕“clear”追踪创建、进入临界区/协议帧、状态提交、异常转换和 finally 释放位置。

## 5. 为什么这样实现

这样避免线程复用串号，并明确监听器失败是否影响主业务。

若省略该节点，问题通常不会在单线程 happy path 暴露，而会在高并发、慢下游、客户端断开或关闭阶段表现为乱序、泄漏、死锁或假成功。

## 6. 完整代码实现

以下机制代码已通过 Java 21 编译检查：

```java
import java.util.*;
import java.util.concurrent.*;
import java.util.concurrent.CopyOnWriteArrayList;
import java.util.function.Consumer;

public class ContextEventBusDemo implements AutoCloseable {
    static final ThreadLocal<Map<String,String>> CTX = ThreadLocal.withInitial(Map::of);
    private final ExecutorService executor = Executors.newFixedThreadPool(2);
    private final CopyOnWriteArrayList<Consumer<String>> listeners = new CopyOnWriteArrayList<>();

    void subscribe(Consumer<String> listener) { listeners.add(listener); }
    CompletableFuture<Void> publish(String event) {
        Map<String,String> captured = Map.copyOf(CTX.get());
        return CompletableFuture.runAsync(() -> {
            Map<String,String> previous = CTX.get();
            CTX.set(captured);
            try {
                for (Consumer<String> listener : listeners) {
                    try { listener.accept(event); }
                    catch (RuntimeException ex) { System.err.println("listener failed: " + ex); }
                }
            } finally { CTX.set(previous); }
        }, executor);
    }
    public void close() { executor.shutdownNow(); }

    public static void main(String[] args) {
        try (var bus = new ContextEventBusDemo()) {
            bus.subscribe(e -> System.out.println(CTX.get().get("session") + ":" + e));
            CTX.set(Map.of("session", "s1"));
            bus.publish("tool-completed").join();
            CTX.remove();
        }
    }
}
```

把与“clear”有关的保护删除或移位，再用 latch/barrier、短 deadline 或强制异常稳定复现失败，不要只用 sleep 猜测时序。

## 7. 疑难点与失败推演

- 线程安全容器不保证跨多步业务不变量；
- timeout 只停止等待，不自动取消底层 I/O；
- 虚拟线程降低线程成本，不提供下游背压；
- 成功事件、状态提交和资源释放的顺序必须能经受中途崩溃。

## 8. 实验与面试

1. 用 `rg "clear" src/main/java src/test/java` 定位直接实现并沿调用链补全间接实现；
2. 画 happens-before、队列或状态迁移图；
3. 制造并发进入、容量耗尽、超时和 shutdown；
4. 面试时按“本质—项目源码—为什么—失败模式—改进”回答。

## 9. 延伸学习

- [SLF4J Manual](https://www.slf4j.org/manual.html)：重点理解 MDC 与日志门面的职责边界。
- [OpenTelemetry Context and Traces](https://opentelemetry.io/docs/concepts/signals/traces/)：学习跨线程/跨进程 context propagation，以及 MDC 与 trace context 的区别。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段二：并发、网络与取消
- **完整路线周次**：第 13/42 周
- **专题内进度**：第 4/12 个节点
- **全课程进度**：第 150/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[restore：底层原理、源码与实现](03.md)
- **下一知识点**：[publish：底层原理、源码与实现](05.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

