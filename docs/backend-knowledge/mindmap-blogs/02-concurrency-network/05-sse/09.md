# heartbeat：底层原理、源码与实现

> 所属专题：[SSE 协议与流式响应](../../../02-concurrency-network/05-sse.md)  
> 导图路径：工程问题 → heartbeat  
> 类型：SSE 传输与流控制节点；编号 9/12

## 1. 学习结论

“heartbeat”是SSE 传输与流控制节点。掌握标准是能从线程/状态/协议层说明正常路径、竞争路径、超时路径和关闭路径。

## 2. 概念与项目语境

- 用注释行 `: heartbeat\n\n` 保活；
- 代理可能缓冲响应，需要关闭 buffering；
- EventSource 断线重连会重复事件，需 id/幂等；
- 客户端慢会阻塞写或撑大队列，必须限制缓冲；
- CORS、Origin、认证 Token 仍必须处理；
- 连接关闭时传播取消，避免后台继续计费。

一个事件可有多行 `data:`，客户端用换行拼接；以冒号开头是注释，可作 heartbeat；`id:` 更新重连的 Last-Event-ID；`retry:` 建议客户端重连间隔。数据中包含换行时必须拆成多行 data，不能直接破坏空行分帧。

## 3. 底层实现原理

HTTP body 中以 UTF-8 字段和空行 framing 事件，写入必须 frame 级串行并处理 flush、心跳、断开和出站背压。

分析任何并发实现都要写出：共享状态由谁拥有；哪些操作必须原子；可见性由锁、volatile、原子类还是队列保证；容量耗尽如何传播；取消后谁负责释放实际资源。

## 4. HippoBuddy 源码追踪

- [SseWriter.java](../../../../../src/main/java/com/example/agent/web/util/SseWriter.java)
- [Event.java](../../../../../src/main/java/com/example/agent/core/event/Event.java)

```java
private final BlockingQueue<String> queue = new LinkedBlockingQueue<>(2048);
private volatile boolean disconnected = false;

public void sendSseEvent(String event, String data) {
    if (closed || disconnected) return;
    String payload = "event: " + event + "\n" + "data: " + data + "\n\n";
    if (!queue.offer(payload)) {
        disconnected = true;
        queue.clear();
        return;
    }
    enqueued.incrementAndGet();
}
```

围绕“heartbeat”追踪创建、进入临界区/协议帧、状态提交、异常转换和 finally 释放位置。

## 5. 为什么这样实现

这样客户端才能增量解析且错误可在 HTTP 200 后用流内事件表达。

若省略该节点，问题通常不会在单线程 happy path 暴露，而会在高并发、慢下游、客户端断开或关闭阶段表现为乱序、泄漏、死锁或假成功。

## 6. 完整代码实现

以下机制代码已通过 Java 21 编译检查：

```java
import java.io.*;
import java.nio.charset.StandardCharsets;
import java.util.concurrent.locks.ReentrantLock;

final class SseEmitter implements Closeable {
    private final OutputStream out;
    private final ReentrantLock writeLock = new ReentrantLock();
    SseEmitter(OutputStream out) { this.out = out; }

    void send(String event, String id, String data) throws IOException {
        if (event != null && (event.contains("\r") || event.contains("\n")))
            throw new IllegalArgumentException("invalid event name");
        if (id != null && (id.contains("\r") || id.contains("\n")))
            throw new IllegalArgumentException("invalid id");
        StringBuilder frame = new StringBuilder();
        if (event != null) frame.append("event: ").append(event).append('\n');
        if (id != null) frame.append("id: ").append(id).append('\n');
        String normalized = data == null ? "" : data.replace("\r\n", "\n").replace('\r', '\n');
        for (String line : normalized.split("\n", -1)) frame.append("data: ").append(line).append('\n');
        frame.append('\n');

        byte[] bytes = frame.toString().getBytes(StandardCharsets.UTF_8);
        writeLock.lock();
        try { out.write(bytes); out.flush(); }
        finally { writeLock.unlock(); }
    }
    public void close() throws IOException { out.close(); }

    public static void main(String[] args) throws Exception {
        var bytes = new ByteArrayOutputStream();
        try (var sse = new SseEmitter(bytes)) { sse.send("delta", "7", "a\nb"); }
        String wire = bytes.toString(StandardCharsets.UTF_8);
        if (!wire.equals("event: delta\nid: 7\ndata: a\ndata: b\n\n")) throw new AssertionError(wire);
    }
}
```

把与“heartbeat”有关的保护删除或移位，再用 latch/barrier、短 deadline 或强制异常稳定复现失败，不要只用 sleep 猜测时序。

## 7. 疑难点与失败推演

- 线程安全容器不保证跨多步业务不变量；
- timeout 只停止等待，不自动取消底层 I/O；
- 虚拟线程降低线程成本，不提供下游背压；
- 成功事件、状态提交和资源释放的顺序必须能经受中途崩溃。

## 8. 实验与面试

1. 用 `rg "heartbeat" src/main/java src/test/java` 定位直接实现并沿调用链补全间接实现；
2. 画 happens-before、队列或状态迁移图；
3. 制造并发进入、容量耗尽、超时和 shutdown；
4. 面试时按“本质—项目源码—为什么—失败模式—改进”回答。

## 9. 延伸学习

- [MDN：Using server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events)：精读 event stream format、断线重连和连接数限制。
- [OpenAI Streaming Events API](https://platform.openai.com/docs/api-reference/responses-streaming)：对照 typed event、sequence number 和终态事件设计。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段二：并发、网络与取消
- **完整路线周次**：第 10/42 周
- **专题内进度**：第 9/12 个节点
- **全课程进度**：第 119/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[flush：底层原理、源码与实现](08.md)
- **下一知识点**：[proxy timeout：底层原理、源码与实现](10.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

