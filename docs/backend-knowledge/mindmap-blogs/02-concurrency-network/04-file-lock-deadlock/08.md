# 逆序释放：底层原理、源码与实现

> 所属专题：[多文件锁与死锁](../../../02-concurrency-network/04-file-lock-deadlock.md)  
> 导图路径：死锁预防 → 逆序释放  
> 类型：互斥、原子性与锁顺序节点；编号 8/12

## 1. 学习结论

“逆序释放”是互斥、原子性与锁顺序节点。掌握标准是能从线程/状态/协议层说明正常路径、竞争路径、超时路径和关闭路径。

## 2. 概念与项目语境

`FileLockManager` 将路径转绝对路径并 normalize，去重后按字符串排序，依次获取 ReentrantLock，finally 逆序释放。所有任务遵循同一个全序，无法形成 A→B→A 环。

锁身份必须来自 canonical resource identity，而不是调用者原始字符串；`a/../b`、大小写别名、symlink 若映射到同一文件却拿到不同锁，会形成“看似加锁但仍并发”。多资源操作先解析全部身份、去重并按稳定全序获取；中途失败逆序释放，任何 Tool 执行期间都不得再以相反顺序临时加锁。

## 3. 底层实现原理

同一锁的 unlock/lock 建立 happens-before；多锁按 canonical key 全局排序获取、逆序释放以消除循环等待。

分析任何并发实现都要写出：共享状态由谁拥有；哪些操作必须原子；可见性由锁、volatile、原子类还是队列保证；容量耗尽如何传播；取消后谁负责释放实际资源。

## 4. HippoBuddy 源码追踪

- [FileLockManager.java](../../../../../src/main/java/com/example/agent/tools/concurrent/FileLockManager.java)
- [Tool.java](../../../../../src/main/java/com/example/agent/llm/model/Tool.java)

```java
for (String p : filePaths) {
    String n = normalizePath(p);
    if (!normalized.contains(n)) normalized.add(n);
}
normalized.sort(String::compareTo);

try {
    for (String n : normalized) {
        ReentrantLock lock = fileLocks.computeIfAbsent(n, k -> new ReentrantLock());
        lock.lock();
        acquired.add(lock);
    }
    return action.run();
} finally {
    for (int i = acquired.size() - 1; i >= 0; i--) acquired.get(i).unlock();
}
```

围绕“逆序释放”追踪创建、进入临界区/协议帧、状态提交、异常转换和 finally 释放位置。

## 5. 为什么这样实现

这样维持复合状态不变量并避免死锁；线程安全集合无法替代跨多步操作的原子边界。

若省略该节点，问题通常不会在单线程 happy path 暴露，而会在高并发、慢下游、客户端断开或关闭阶段表现为乱序、泄漏、死锁或假成功。

## 6. 完整代码实现

以下机制代码已通过 Java 21 编译检查：

```java
import java.nio.file.*;
import java.time.*;
import java.util.*;
import java.util.concurrent.*;
import java.util.concurrent.locks.ReentrantLock;

public final class OrderedFileLocks {
    private final ConcurrentHashMap<Path, ReentrantLock> locks = new ConcurrentHashMap<>();

    <T> T withLocks(Collection<Path> input, Duration timeout, Callable<T> action) throws Exception {
        List<Path> keys = input.stream().map(p -> p.toAbsolutePath().normalize()).distinct()
                .sorted(Comparator.comparing(Path::toString)).toList();
        List<ReentrantLock> acquired = new ArrayList<>();
        long deadline = System.nanoTime() + timeout.toNanos();
        try {
            for (Path key : keys) {
                ReentrantLock lock = locks.computeIfAbsent(key, ignored -> new ReentrantLock());
                long remaining = deadline - System.nanoTime();
                if (remaining <= 0 || !lock.tryLock(remaining, TimeUnit.NANOSECONDS))
                    throw new TimeoutException("cannot lock " + key);
                acquired.add(lock);
            }
            return action.call();                       // 锁内必须重新校验文件版本
        } finally {
            for (int i = acquired.size() - 1; i >= 0; i--) acquired.get(i).unlock();
        }
    }
}
```

把与“逆序释放”有关的保护删除或移位，再用 latch/barrier、短 deadline 或强制异常稳定复现失败，不要只用 sleep 猜测时序。

## 7. 疑难点与失败推演

- 线程安全容器不保证跨多步业务不变量；
- timeout 只停止等待，不自动取消底层 I/O；
- 虚拟线程降低线程成本，不提供下游背压；
- 成功事件、状态提交和资源释放的顺序必须能经受中途崩溃。

## 8. 实验与面试

1. 用 `rg "逆序释放" src/main/java src/test/java` 定位直接实现并沿调用链补全间接实现；
2. 画 happens-before、队列或状态迁移图；
3. 制造并发进入、容量耗尽、超时和 shutdown；
4. 面试时按“本质—项目源码—为什么—失败模式—改进”回答。

## 9. 延伸学习

- [Oracle Java Concurrency Guide](https://docs.oracle.com/en/java/javase/21/core/concurrency.html)：结合锁、原子变量和并发集合理解每种机制的保证范围。
- [Java Concurrency in Practice](https://jcip.net/)：重点读 Liveness Hazards、Deadlock、Lock Ordering。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段二：并发、网络与取消
- **完整路线周次**：第 9/42 周
- **专题内进度**：第 8/12 个节点
- **全课程进度**：第 106/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[顺序加锁：底层原理、源码与实现](07.md)
- **下一知识点**：[单 JVM：底层原理、源码与实现](09.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

