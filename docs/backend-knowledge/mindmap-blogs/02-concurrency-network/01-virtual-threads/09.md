# 连接仍稀缺：底层原理、源码与实现

> 所属专题：[Java 21 虚拟线程](../../../02-concurrency-network/01-virtual-threads.md)  
> 导图路径：边界 → 连接仍稀缺  
> 类型：“Java 21 虚拟线程”中的并发控制节点；编号 9/11

## 1. 学习结论

“连接仍稀缺”是“Java 21 虚拟线程”中的并发控制节点。掌握标准是能从线程/状态/协议层说明正常路径、竞争路径、超时路径和关闭路径。

## 2. 概念与项目语境

虚拟线程的创建、挂起和恢复可以拆成三个不同对象：`VirtualThread` 保存 Java Thread 身份；continuation 保存可恢复的 Java 栈片段；carrier 是实际执行机器指令的平台线程。任务调用支持虚拟线程的阻塞 API 时，JDK 把 continuation 从 carrier 卸载，注册 I/O 等待，carrier 转去运行别的 continuation。就绪后只是重新进入调度队列，并不保证回到原 carrier。

`ThreadPools` 把 Tool 和普通异步任务交给 `newVirtualThreadPerTaskExecutor()`，意味着 executor 的 submit 不会像固定池那样通过有限 worker 数形成背压：每个已接纳任务都能拥有一个虚拟线程。因此它解决的是“等待占用平台线程”，不是“限制请求数”。MCP cleanup/scheduler 使用单平台线程，则是因为定时任务需要少量稳定 worker 和串行时间语义。

## 3. 底层实现原理

底层通过明确所有权、顺序、容量、时间预算和可见性关系约束并发执行。

分析任何并发实现都要写出：共享状态由谁拥有；哪些操作必须原子；可见性由锁、volatile、原子类还是队列保证；容量耗尽如何传播；取消后谁负责释放实际资源。

## 4. HippoBuddy 源码追踪

- [ThreadPools.java](../../../../../src/main/java/com/example/agent/core/concurrency/ThreadPools.java)
- [Tool.java](../../../../../src/main/java/com/example/agent/llm/model/Tool.java)

```java
EXECUTORS.put(Names.TOOL_EXECUTOR,
        Executors.newVirtualThreadPerTaskExecutor());
EXECUTORS.put(Names.ASYNC_GENERAL,
        Executors.newVirtualThreadPerTaskExecutor());

EXECUTORS.put(Names.MCP_SCHEDULER,
        Executors.newSingleThreadScheduledExecutor(
            namedThreadFactory("mcp-scheduler", true)));
EXECUTORS.put(Names.JSONRPC_CLEANUP,
        Executors.newSingleThreadScheduledExecutor(
            namedThreadFactory("jsonrpc-cleanup", true)));
```

围绕“连接仍稀缺”追踪创建、进入临界区/协议帧、状态提交、异常转换和 finally 释放位置。

## 5. 为什么这样实现

这样实现让竞态、过载、取消和关闭都有确定语义。

若省略该节点，问题通常不会在单线程 happy path 暴露，而会在高并发、慢下游、客户端断开或关闭阶段表现为乱序、泄漏、死锁或假成功。

## 6. 完整代码实现

以下机制代码已通过 Java 21 编译检查：

```java
import java.time.Duration;
import java.util.concurrent.*;

public class BoundedVirtualThreadsDemo {
    static final class Client implements AutoCloseable {
        private final ExecutorService tasks = Executors.newVirtualThreadPerTaskExecutor();
        private final Semaphore permits;
        Client(int maxConcurrentCalls) { permits = new Semaphore(maxConcurrentCalls, true); }

        Future<String> call(int id, Duration deadline) {
            return tasks.submit(() -> {
                if (!permits.tryAcquire(deadline.toMillis(), TimeUnit.MILLISECONDS))
                    throw new TimeoutException("queue deadline exceeded: " + id);
                try {
                    Thread.sleep(100);                 // 模拟可卸载的远程 I/O
                    return "ok-" + id;
                } finally {
                    permits.release();
                }
            });
        }
        public void close() throws InterruptedException {
            tasks.shutdown();
            if (!tasks.awaitTermination(2, TimeUnit.SECONDS)) tasks.shutdownNow();
        }
    }

    public static void main(String[] args) throws Exception {
        try (Client client = new Client(10)) {
            var futures = new java.util.ArrayList<Future<String>>();
            for (int i = 0; i < 100; i++) futures.add(client.call(i, Duration.ofSeconds(2)));
            for (Future<String> f : futures) System.out.println(f.get());
        }
    }
}
```

把与“连接仍稀缺”有关的保护删除或移位，再用 latch/barrier、短 deadline 或强制异常稳定复现失败，不要只用 sleep 猜测时序。

## 7. 疑难点与失败推演

- 线程安全容器不保证跨多步业务不变量；
- timeout 只停止等待，不自动取消底层 I/O；
- 虚拟线程降低线程成本，不提供下游背压；
- 成功事件、状态提交和资源释放的顺序必须能经受中途崩溃。

## 8. 实验与面试

1. 用 `rg "连接仍稀缺" src/main/java src/test/java` 定位直接实现并沿调用链补全间接实现；
2. 画 happens-before、队列或状态迁移图；
3. 制造并发进入、容量耗尽、超时和 shutdown；
4. 面试时按“本质—项目源码—为什么—失败模式—改进”回答。

## 9. 延伸学习

- [OpenJDK JEP 444：Virtual Threads](https://openjdk.org/jeps/444)：重点读 Goals、Non-Goals、Scheduling 和不要池化虚拟线程的原因。
- [Oracle Java 21 Virtual Threads Guide](https://docs.oracle.com/en/java/javase/21/core/virtual-threads.html)：重点学习 pinning、JFR 事件和 thread dump。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段二：并发、网络与取消
- **完整路线周次**：第 6/42 周
- **专题内进度**：第 9/11 个节点
- **全课程进度**：第 74/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[CPU 不会变多：底层原理、源码与实现](08.md)
- **下一知识点**：[Pinning：底层原理、源码与实现](10.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

