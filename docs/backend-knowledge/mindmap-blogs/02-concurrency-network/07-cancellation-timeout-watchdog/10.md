# process：底层原理、源码与实现

> 所属专题：[取消、超时、线程中断与 Watchdog](../../../02-concurrency-network/07-cancellation-timeout-watchdog.md)  
> 导图路径：Resource Close → process  
> 类型：取消与时间预算节点；编号 10/11

## 1. 学习结论

“process”是取消与时间预算节点。掌握标准是能从线程/状态/协议层说明正常路径、竞争路径、超时路径和关闭路径。

## 2. 概念与项目语境

SessionCancelManager 保存取消标记；Orchestrator 在 LLM、工具和轮次边界检查；BashProcessManager 管理进程。`IdleTimeoutInputStream` 用 daemon watchdog 每 500ms 检查最后读取时间，超时就 close 底层流，让阻塞 read 抛出异常。

一个 chat request 派生 LLM 流、多个 Tool、Bash 进程、SubAgent。取消应从父 scope 向全部子任务传播，父任务只有在子任务终止/隔离后才结束。若只设置 session flag，正在阻塞的 HTTP read 和 Process.waitFor 不检查标志，仍会泄漏。

## 3. 底层实现原理

入口计算绝对 deadline，各层传剩余时间；取消组合状态标志、interrupt 与资源 close/destroy 解除实际阻塞。

分析任何并发实现都要写出：共享状态由谁拥有；哪些操作必须原子；可见性由锁、volatile、原子类还是队列保证；容量耗尽如何传播；取消后谁负责释放实际资源。

## 4. HippoBuddy 源码追踪

- [SessionCancelManager.java](../../../../../src/main/java/com/example/agent/web/session/SessionCancelManager.java)
- [IdleTimeoutInputStream.java](../../../../../src/main/java/com/example/agent/llm/stream/IdleTimeoutInputStream.java)
- [BashProcessManager.java](../../../../../src/main/java/com/example/agent/tools/BashProcessManager.java)

```java
if (idle > timeoutMs) {
    timedOut = true;
    try {
        in.close(); // 让阻塞中的 read() 退出
    } catch (IOException ignored) {
    }
    return;
}

private IOException translateIfTimedOut(IOException e) {
    if (timedOut) {
        return new SocketTimeoutException("流式响应读取空闲超时");
    }
    return e;
}
```

围绕“process”追踪创建、进入临界区/协议帧、状态提交、异常转换和 finally 释放位置。

## 5. 为什么这样实现

这样防止调用方超时后后台工作继续花费资源或产生迟到副作用。

若省略该节点，问题通常不会在单线程 happy path 暴露，而会在高并发、慢下游、客户端断开或关闭阶段表现为乱序、泄漏、死锁或假成功。

## 6. 完整代码实现

以下机制代码已通过 Java 21 编译检查：

```java
import java.time.Duration;
import java.util.concurrent.*;
import java.util.concurrent.atomic.AtomicBoolean;

public class CancellationDemo {
    static final class Deadline {
        private final long end = System.nanoTime();
        private final long timeoutNanos;
        Deadline(Duration timeout) { timeoutNanos = timeout.toNanos(); }
        long remainingNanos() { return Math.max(0, timeoutNanos - (System.nanoTime() - end)); }
        void throwIfExpired() throws TimeoutException {
            if (remainingNanos() == 0) throw new TimeoutException("deadline exceeded");
        }
    }
    static final class Cancellation {
        private final AtomicBoolean cancelled = new AtomicBoolean();
        private volatile Thread owner;
        void attachCurrentThread() { owner = Thread.currentThread(); }
        void cancel() { cancelled.set(true); Thread t = owner; if (t != null) t.interrupt(); }
        void check() throws InterruptedException {
            if (cancelled.get() || Thread.currentThread().isInterrupted())
                throw new InterruptedException("cancelled");
        }
    }
    static String work(Deadline d, Cancellation c) throws Exception {
        c.attachCurrentThread();
        for (int i = 0; i < 100; i++) {
            c.check(); d.throwIfExpired();
            long wait = Math.min(TimeUnit.NANOSECONDS.toMillis(d.remainingNanos()), 50);
            Thread.sleep(Math.max(1, wait));
        }
        return "done";
    }
    public static void main(String[] args) throws Exception {
        Cancellation c = new Cancellation();
        try (ExecutorService e = Executors.newVirtualThreadPerTaskExecutor()) {
            Future<String> f = e.submit(() -> work(new Deadline(Duration.ofSeconds(5)), c));
            c.cancel();
            try { f.get(); } catch (ExecutionException expected) {
                if (!(expected.getCause() instanceof InterruptedException)) throw expected;
            }
        }
    }
}
```

把与“process”有关的保护删除或移位，再用 latch/barrier、短 deadline 或强制异常稳定复现失败，不要只用 sleep 猜测时序。

## 7. 疑难点与失败推演

- 线程安全容器不保证跨多步业务不变量；
- timeout 只停止等待，不自动取消底层 I/O；
- 虚拟线程降低线程成本，不提供下游背压；
- 成功事件、状态提交和资源释放的顺序必须能经受中途崩溃。

## 8. 实验与面试

1. 用 `rg "process" src/main/java src/test/java` 定位直接实现并沿调用链补全间接实现；
2. 画 happens-before、队列或状态迁移图；
3. 制造并发进入、容量耗尽、超时和 shutdown；
4. 面试时按“本质—项目源码—为什么—失败模式—改进”回答。

## 9. 延伸学习

- [Java `Future` API](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/util/concurrent/Future.html)：重点理解 `cancel(true)` 只是尝试中断，并不保证任务已经停止。
- [Java Concurrency in Practice](https://jcip.net/)：重点读 Cancellation、Interruption、Shutdown 和处理不可中断阻塞。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段二：并发、网络与取消
- **完整路线周次**：第 12/42 周
- **专题内进度**：第 10/11 个节点
- **全课程进度**：第 145/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[HTTP body：底层原理、源码与实现](09.md)
- **下一知识点**：[future：底层原理、源码与实现](11.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

