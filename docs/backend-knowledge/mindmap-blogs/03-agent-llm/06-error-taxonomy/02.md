# connect：底层原理、源码与实现

> 所属专题：[错误分类与异常语义](../../../03-agent-llm/06-error-taxonomy.md)  
> 导图路径：Transport → connect  
> 类型：错误分类节点；编号 2/16

## 1. 学习结论

“connect”是错误分类节点。掌握它不是背 API，而是能解释协议字段、运行时状态、可信边界、终止条件和失败恢复。

## 2. 概念与项目语境

底层抛 IOException，LLM Adapter 翻译为 LlmConnectionException；应用层再转换为稳定 error event。不要在最底层直接生成前端中文，也不要让 Handler 解析供应商 body。

## 3. 底层实现原理

边界异常被归一为稳定 category、retryable、status、requestId 与安全 message，同时保留 cause 供受控诊断。

源码阅读依次回答：输入如何从 wire model 变为 canonical model；哪个对象拥有本步状态；流式片段如何闭合；错误是否可重试；动作与 ToolResult 如何关联；取消或崩溃后如何恢复。

## 4. HippoBuddy 源码追踪

- [LlmErrorClassifier.java](../../../../../src/main/java/com/example/agent/llm/exception/LlmErrorClassifier.java)
- [LlmError.java](../../../../../src/main/java/com/example/agent/llm/exception/LlmError.java)
- [LlmApiException.java](../../../../../src/main/java/com/example/agent/llm/exception/LlmApiException.java)
- [LlmConnectionException.java](../../../../../src/main/java/com/example/agent/llm/exception/LlmConnectionException.java)

```java
public static LlmError classify(String provider, int statusCode, String body) {
    LlmError error = classifyProviderSpecific(provider, statusCode, body);
    if (error != null) return error;
    error = classifyFromBodyType(body);
    if (error != null) return error;
    error = classifyByText(body);
    if (error != null) return error;
    error = classifyByStatus(statusCode, body);
    if (error != null) return error;
    return new LlmError(CODE_UNKNOWN, fallbackMessage(body, statusCode), detail(body));
}
```

围绕“connect”追踪请求构造、状态写入、Provider/Tool 边界、异常转换和终态发布。

## 5. 为什么这样实现

这样上层能作重试/终止决策，用户输出又不会泄露原始响应、Prompt 或 secret。

若把该责任交给 Prompt 或调用者约定，模型输出、网络切片和失败重试会破坏顺序与配对，产生重复工具、副作用未知或不可恢复会话。

## 6. 完整代码实现

以下机制实现已经通过 Java 21 编译检查：

```java
import java.util.*;

public class ErrorTaxonomyDemo {
    enum Category { AUTH, RATE_LIMIT, TIMEOUT, NETWORK, INVALID_REQUEST, PROVIDER, INTERNAL }
    record AppError(Category category, boolean retryable, Integer status,
                    String requestId, String safeMessage, Throwable cause) {}

    static AppError classify(int status, String requestId, String rawBody, Throwable cause) {
        return switch (status) {
            case 401, 403 -> new AppError(Category.AUTH, false, status, requestId,
                    "模型服务认证失败，请检查配置", cause);
            case 408 -> new AppError(Category.TIMEOUT, true, status, requestId,
                    "模型服务响应超时", cause);
            case 429 -> new AppError(Category.RATE_LIMIT, true, status, requestId,
                    "请求过于频繁，请稍后重试", cause);
            default -> status >= 500
                    ? new AppError(Category.PROVIDER, true, status, requestId, "模型服务暂时不可用", cause)
                    : new AppError(Category.INVALID_REQUEST, false, status, requestId, "请求不受支持", cause);
        };
    }
    static Map<String,Object> publicView(AppError e) {
        Map<String,Object> out = new LinkedHashMap<>();
        out.put("code", e.category().name()); out.put("message", e.safeMessage());
        if (e.requestId() != null) out.put("requestId", e.requestId());
        out.put("retryable", e.retryable());
        return Map.copyOf(out);                         // 不包含 rawBody/cause
    }
    public static void main(String[] args) {
        AppError e = classify(401, "req-7", "api-key=secret", null);
        String shown = publicView(e).toString();
        if (shown.contains("secret") || e.retryable()) throw new AssertionError(shown);
    }
}
```

定位与“connect”相关的状态字段和 invariant；删除校验、拆分 arguments 或制造部分成功，观察失败如何出现。

## 7. 疑难点与失败推演

- 模型输出是建议，不是授权；Schema 也不是安全边界；
- 流式 partial data 未闭合前不能执行；
- timeout 结果可能未知，重试必须结合幂等语义；
- 本地估算、Provider Usage、缓存账单和实际费用必须分账。

## 8. 实验与面试

1. 用 `rg "connect" src/main/java src/test/java` 定位直接或相关实现；
2. 画 wire → canonical → runtime state → observation 的路径；
3. 注入切片、429、取消、重复 callId 或 usage 缺失；
4. 按“概念—源码—底层状态—为什么—失败与改进”口述。

## 9. 延伸学习

- [OpenAI API Error Codes](https://platform.openai.com/docs/guides/error-codes)：对照 HTTP 状态、原因和推荐动作。
- [Anthropic API Errors](https://docs.anthropic.com/en/api/errors)：重点比较 429、500、529 及 request-id，完善 Provider 特例测试。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段三：Agent 与 LLM 协议
- **完整路线周次**：第 19/42 周
- **专题内进度**：第 2/16 个节点
- **全课程进度**：第 223/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[DNS：底层原理、源码与实现](01.md)
- **下一知识点**：[TLS：底层原理、源码与实现](03.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

