# jitter：底层原理、源码与实现

> 所属专题：[重试、指数退避与幂等](../../../03-agent-llm/05-retry-backoff-idempotency.md)  
> 导图路径：时间策略 → jitter  
> 类型：重试与幂等节点；编号 6/10

## 1. 学习结论

“jitter”是重试与幂等节点。掌握它不是背 API，而是能解释协议字段、运行时状态、可信边界、终止条件和失败恢复。

## 2. 概念与项目语境

第 n 次延迟 `min(maxDelay, initial × multiplier^n)`。没有 jitter 时，成千客户端会在 1/2/4 秒同时撞击恢复中的服务，称 thundering herd。Full jitter 可用 `[0, calculatedDelay]` 随机值。

- [ ] 能区分 transient/permanent/ambiguous；
- [ ] 能写 full jitter；
- [ ] 能解释 timeout 不代表未执行；
- [ ] 能设计 ToolCall 幂等键。

## 3. 底层实现原理

先按错误语义分类，再用指数退避、Full Jitter、Retry-After 和总 deadline 决定下一 attempt；副作用用幂等键去重。

源码阅读依次回答：输入如何从 wire model 变为 canonical model；哪个对象拥有本步状态；流式片段如何闭合；错误是否可重试；动作与 ToolResult 如何关联；取消或崩溃后如何恢复。

## 4. HippoBuddy 源码追踪

- [RetryPolicy.java](../../../../../src/main/java/com/example/agent/llm/retry/RetryPolicy.java)
- [AbstractLlmClient.java](../../../../../src/main/java/com/example/agent/llm/client/AbstractLlmClient.java)
- [ToolCall.java](../../../../../src/main/java/com/example/agent/llm/model/ToolCall.java)

```java
public long getDelayMs(int attempt) {
    if (attempt < 0) return initialDelayMs;
    if (attempt > 30) return maxDelayMs;
    double delay = initialDelayMs * Math.pow(backoffMultiplier, attempt);
    if (delay > maxDelayMs || delay < 0
            || Double.isInfinite(delay) || Double.isNaN(delay)) {
        return maxDelayMs;
    }
    return Math.min((long) delay, maxDelayMs);
}

public boolean shouldRetry(LlmException e, int attempt) {
    if (attempt >= maxRetries) return false;
    return e instanceof LlmTimeoutException
        || e instanceof LlmConnectionException
        || (e instanceof LlmApiException api
            && (api.isServerError() || api.isRateLimited()));
}
```

围绕“jitter”追踪请求构造、状态写入、Provider/Tool 边界、异常转换和终态发布。

## 5. 为什么这样实现

这样避免永久错误无效重试、同步风暴和超时结果未知导致的重复副作用。

若把该责任交给 Prompt 或调用者约定，模型输出、网络切片和失败重试会破坏顺序与配对，产生重复工具、副作用未知或不可恢复会话。

## 6. 完整代码实现

以下机制实现已经通过 Java 21 编译检查：

```java
import java.time.*;
import java.util.concurrent.*;
import java.util.function.Supplier;

public class RetryDemo {
    static final class RemoteException extends RuntimeException {
        final int status;
        RemoteException(int status) { this.status = status; }
    }
    static <T> T execute(Supplier<T> call, int maxAttempts, Duration totalTimeout) {
        long deadline = System.nanoTime() + totalTimeout.toNanos();
        RuntimeException last = null;
        for (int attempt = 0; attempt < maxAttempts; attempt++) {
            try { return call.get(); }
            catch (RemoteException e) {
                last = e;
                if (!(e.status == 408 || e.status == 429 || e.status >= 500)) throw e;
            }
            long upperMillis = Math.min(5_000L, 100L << Math.min(attempt, 10));
            long sleepMillis = ThreadLocalRandom.current().nextLong(upperMillis + 1);
            long remaining = deadline - System.nanoTime();
            if (remaining <= TimeUnit.MILLISECONDS.toNanos(sleepMillis)) break;
            try { Thread.sleep(sleepMillis); }
            catch (InterruptedException e) { Thread.currentThread().interrupt(); throw new CancellationException(); }
        }
        throw last != null ? last : new IllegalStateException("no attempt");
    }
    public static void main(String[] args) {
        var count = new java.util.concurrent.atomic.AtomicInteger();
        String value = execute(() -> {
            if (count.incrementAndGet() < 3) throw new RemoteException(503);
            return "ok";
        }, 4, Duration.ofSeconds(3));
        if (!value.equals("ok") || count.get() != 3) throw new AssertionError();
    }
}
```

定位与“jitter”相关的状态字段和 invariant；删除校验、拆分 arguments 或制造部分成功，观察失败如何出现。

## 7. 疑难点与失败推演

- 模型输出是建议，不是授权；Schema 也不是安全边界；
- 流式 partial data 未闭合前不能执行；
- timeout 结果可能未知，重试必须结合幂等语义；
- 本地估算、Provider Usage、缓存账单和实际费用必须分账。

## 8. 实验与面试

1. 用 `rg "jitter" src/main/java src/test/java` 定位直接或相关实现；
2. 画 wire → canonical → runtime state → observation 的路径；
3. 注入切片、429、取消、重复 callId 或 usage 缺失；
4. 按“概念—源码—底层状态—为什么—失败与改进”口述。

## 9. 延伸学习

- [AWS：Exponential Backoff and Jitter](https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/)：重点理解 full/equal/decorrelated jitter 对拥塞的影响。
- [Release It!（O’Reilly）](https://www.oreilly.com/library/view/release-it-2nd/9781680504552/)：重点读 Timeout、Circuit Breaker、Bulkhead 和稳定性模式。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段三：Agent 与 LLM 协议
- **完整路线周次**：第 18/42 周
- **专题内进度**：第 6/10 个节点
- **全课程进度**：第 217/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[max delay：底层原理、源码与实现](05.md)
- **下一知识点**：[Retry-After：底层原理、源码与实现](07.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

