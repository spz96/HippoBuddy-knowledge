# tool schemas：底层原理、源码与实现

> 所属专题：[Prompt 前缀缓存](../../../03-agent-llm/07-prompt-cache.md)  
> 导图路径：稳定内容 → tool schemas  
> 类型：Function Calling 协议节点；编号 5/13

## 1. 学习结论

“tool schemas”是Function Calling 协议节点。掌握它不是背 API，而是能解释协议字段、运行时状态、可信边界、终止条件和失败恢复。

## 2. 概念与项目语境

WebAgentOrchestrator 为同 session 冻结当前 AgentMode 的 Tool Schema 快照，避免动态注册/Map 顺序导致每轮工具前缀变化；Usage 记录 cache read/miss 和命中率，异常低时告警。

分母应只包含可缓存前缀 token；新会话和短 Prompt 不告警。观察每 turn 的 cacheRead 曲线，正常长会话应随稳定前缀增长。突然降为 0 时比较 prompt hash/tool version/rule version。还要结合 input price，因为不同 provider cache discount 不同。

## 3. 底层实现原理

ToolCall 由 name、arguments、callId 组成；Runtime 依次做 JSON/Schema/业务/权限校验，并用原 callId 追加 ToolResult。

源码阅读依次回答：输入如何从 wire model 变为 canonical model；哪个对象拥有本步状态；流式片段如何闭合；错误是否可重试；动作与 ToolResult 如何关联；取消或崩溃后如何恢复。

## 4. HippoBuddy 源码追踪

- [WebAgentOrchestrator.java](../../../../../src/main/java/com/example/agent/web/orchestrator/WebAgentOrchestrator.java)
- [Usage.java](../../../../../src/main/java/com/example/agent/llm/model/Usage.java)
- [ToolsSnapshotTest.java](../../../../../src/test/java/com/example/agent/web/orchestrator/ToolsSnapshotTest.java)
- [AgentMode.java](../../../../../src/main/java/com/example/agent/core/AgentMode.java)
- [Tool.java](../../../../../src/main/java/com/example/agent/llm/model/Tool.java)

```java
List<Tool> getOrCreateToolsSnapshot(String sessionId, AgentMode mode) {
    FrozenToolsEntry existing = toolsSnapshots.get(sessionId);
    if (existing != null && existing.mode() == mode) {
        return existing.tools();
    }
    List<Tool> tools = toolRegistry.toTools(mode);
    FrozenToolsEntry entry = new FrozenToolsEntry(mode, List.copyOf(tools));
    toolsSnapshots.put(sessionId, entry);
    return entry.tools();
}
```

围绕“tool schemas”追踪请求构造、状态写入、Provider/Tool 边界、异常转换和终态发布。

## 5. 为什么这样实现

这样才能把模型输出限制为受控能力调用，并在重试、并行和恢复时保持动作—观察配对。

若把该责任交给 Prompt 或调用者约定，模型输出、网络切片和失败重试会破坏顺序与配对，产生重复工具、副作用未知或不可恢复会话。

## 6. 完整代码实现

以下机制实现已经通过 Java 21 编译检查：

```java
import java.nio.charset.StandardCharsets;
import java.security.MessageDigest;
import java.util.*;

public class PromptFingerprintDemo {
    record Tool(String name, SortedMap<String,String> schema) {}
    static String canonical(String model, String system, Collection<Tool> tools) {
        StringBuilder out = new StringBuilder();
        out.append("model=").append(model).append('\n');
        out.append("system.len=").append(system.length()).append('\n').append(system).append('\n');
        tools.stream().sorted(Comparator.comparing(Tool::name)).forEach(t -> {
            out.append("tool=").append(t.name()).append('\n');
            t.schema().forEach((k,v) -> out.append(k.length()).append(':').append(k)
                    .append('=').append(v.length()).append(':').append(v).append('\n'));
        });
        return out.toString();
    }
    static String sha256(String value) throws Exception {
        byte[] hash = MessageDigest.getInstance("SHA-256")
                .digest(value.getBytes(StandardCharsets.UTF_8));
        return java.util.HexFormat.of().formatHex(hash);
    }
    public static void main(String[] args) throws Exception {
        Tool a = new Tool("read", new TreeMap<>(Map.of("type", "object", "path", "string")));
        Tool b = new Tool("grep", new TreeMap<>(Map.of("query", "string")));
        String x = sha256(canonical("m", "stable", List.of(a,b)));
        String y = sha256(canonical("m", "stable", List.of(b,a)));
        if (!x.equals(y)) throw new AssertionError("order was not canonicalized");
    }
}
```

定位与“tool schemas”相关的状态字段和 invariant；删除校验、拆分 arguments 或制造部分成功，观察失败如何出现。

## 7. 疑难点与失败推演

- 模型输出是建议，不是授权；Schema 也不是安全边界；
- 流式 partial data 未闭合前不能执行；
- timeout 结果可能未知，重试必须结合幂等语义；
- 本地估算、Provider Usage、缓存账单和实际费用必须分账。

## 8. 实验与面试

1. 用 `rg "tool schemas" src/main/java src/test/java` 定位直接或相关实现；
2. 画 wire → canonical → runtime state → observation 的路径；
3. 注入切片、429、取消、重复 callId 或 usage 缺失；
4. 按“概念—源码—底层状态—为什么—失败与改进”口述。

## 9. 延伸学习

- [OpenAI Prompt Caching Guide](https://platform.openai.com/docs/guides/prompt-caching)：重点学习前缀匹配、cached tokens、cache key 与保留策略。
- [OpenAI API Data Controls](https://platform.openai.com/docs/models/default-usage-policies-by-endpoint)：理解扩展缓存与数据保留/Zero Data Retention 的安全取舍。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段三：Agent 与 LLM 协议
- **完整路线周次**：第 20/42 周
- **专题内进度**：第 5/13 个节点
- **全课程进度**：第 242/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[system prompt：底层原理、源码与实现](04.md)
- **下一知识点**：[rules：底层原理、源码与实现](06.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

