# subagent：底层原理、源码与实现

> 所属专题：[Token Usage、价格与成本治理](../../../03-agent-llm/08-token-cost.md)  
> 导图路径：来源 → subagent  
> 类型：Token 与成本治理节点；编号 9/14

## 1. 学习结论

“subagent”是Token 与成本治理节点。掌握它不是背 API，而是能解释协议字段、运行时状态、可信边界、终止条件和失败恢复。

## 2. 概念与项目语境

成本记账需要把“请求前估算”和“响应后账本”分开。TokenEstimator 用于阻止超限和预估费用；Provider 返回的 Usage 才是已发生请求的主要计量依据。流被取消、usage 缺失或 provider 不报告 cache token 时，账本要标记 estimated/partial，不能悄悄写 0。

一次 Agent Turn 的总成本不是最后一条回答：它包含每个 LLM attempt 的普通 input、cache read/write、output/reasoning，重试也单独收费；父子 Agent 再按 trace/task 归属汇总。工具本身还可能有搜索/API/计算成本，不能全部换算成模型 token 后丢失维度。

## 3. 底层实现原理

请求前 estimator 做保护和预留，响应后 Usage 按普通输入、缓存、输出/推理和价格版本精确记账。

源码阅读依次回答：输入如何从 wire model 变为 canonical model；哪个对象拥有本步状态；流式片段如何闭合；错误是否可重试；动作与 ToolResult 如何关联；取消或崩溃后如何恢复。

## 4. HippoBuddy 源码追踪

- [Usage.java](../../../../../src/main/java/com/example/agent/llm/model/Usage.java)
- [LlmPricing.java](../../../../../src/main/java/com/example/agent/llm/pricing/LlmPricing.java)
- [SessionTokenStats.java](../../../../../src/main/java/com/example/agent/web/session/SessionTokenStats.java)
- [CostMetricsCollector.java](../../../../../src/main/java/com/example/agent/logging/CostMetricsCollector.java)
- [TokenEstimator.java](../../../../../src/main/java/com/example/agent/service/TokenEstimator.java)

```java
public int getCacheReadInputTokens() {
    if (promptCacheHitTokens > 0) return promptCacheHitTokens;
    return promptTokensDetails != null
        ? promptTokensDetails.getCachedTokens() : 0;
}

public double getCacheHitRate() {
    int cacheRead = getCacheReadInputTokens();
    if (cacheRead == 0 || promptTokens == 0) return 0.0;
    return ((double) cacheRead / promptTokens) * 100;
}
```

围绕“subagent”追踪请求构造、状态写入、Provider/Tool 边界、异常转换和终态发布。

## 5. 为什么这样实现

这样并发父子任务可以预留—结算预算，重试和缓存费用也不会被遗漏。

若把该责任交给 Prompt 或调用者约定，模型输出、网络切片和失败重试会破坏顺序与配对，产生重复工具、副作用未知或不可恢复会话。

## 6. 完整代码实现

以下机制实现已经通过 Java 21 编译检查：

```java
import java.math.*;
import java.util.*;

public class TokenCostDemo {
    record Usage(long input, long cachedInput, long output, boolean estimated) {
        Usage { if (input < 0 || cachedInput < 0 || output < 0 || cachedInput > input) throw new IllegalArgumentException(); }
    }
    record Price(BigDecimal inputPerMillion, BigDecimal cachedPerMillion,
                 BigDecimal outputPerMillion, String version) {}
    record Charge(BigDecimal amount, boolean estimated, String priceVersion) {}

    static Charge charge(Usage u, Price p) {
        BigDecimal million = BigDecimal.valueOf(1_000_000);
        long uncached = u.input() - u.cachedInput();
        BigDecimal amount = BigDecimal.valueOf(uncached).multiply(p.inputPerMillion())
                .add(BigDecimal.valueOf(u.cachedInput()).multiply(p.cachedPerMillion()))
                .add(BigDecimal.valueOf(u.output()).multiply(p.outputPerMillion()))
                .divide(million, 12, RoundingMode.HALF_UP);
        return new Charge(amount, u.estimated(), p.version());
    }
    public static void main(String[] args) {
        Price p = new Price(new BigDecimal("3.00"), new BigDecimal("0.30"),
                new BigDecimal("15.00"), "2026-01-01");
        Charge c = charge(new Usage(1_000_000, 200_000, 100_000, false), p);
        if (c.amount().compareTo(new BigDecimal("3.96")) != 0) throw new AssertionError(c);
    }
}
```

定位与“subagent”相关的状态字段和 invariant；删除校验、拆分 arguments 或制造部分成功，观察失败如何出现。

## 7. 疑难点与失败推演

- 模型输出是建议，不是授权；Schema 也不是安全边界；
- 流式 partial data 未闭合前不能执行；
- timeout 结果可能未知，重试必须结合幂等语义；
- 本地估算、Provider Usage、缓存账单和实际费用必须分账。

## 8. 实验与面试

1. 用 `rg "subagent" src/main/java src/test/java` 定位直接或相关实现；
2. 画 wire → canonical → runtime state → observation 的路径；
3. 注入切片、429、取消、重复 callId 或 usage 缺失；
4. 按“概念—源码—底层状态—为什么—失败与改进”口述。

## 9. 延伸学习

- [OpenAI API Pricing](https://platform.openai.com/docs/pricing)：对照输入、缓存输入、输出和工具计费，校准价格版本。
- [OpenAI Token Counting Cookbook](https://github.com/openai/openai-cookbook/blob/main/examples/How_to_count_tokens_with_tiktoken.ipynb)：理解本地估算与服务端真实 usage 的差异。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段三：Agent 与 LLM 协议
- **完整路线周次**：第 21/42 周
- **专题内进度**：第 9/14 个节点
- **全课程进度**：第 259/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[tool result：底层原理、源码与实现](08.md)
- **下一知识点**：[estimate before：底层原理、源码与实现](10.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

