# TokenBudget：从概念到底层源码

> 所属专题：[对象作用域、全局状态与生命周期](../../../01-architecture/03-scope-lifecycle.md)  
> 导图路径：Session → TokenBudget  
> 节点类型：“对象作用域、全局状态与生命周期”中的职责节点；编号 7/12

## 1. 学习结论

“TokenBudget”是“对象作用域、全局状态与生命周期”中的职责节点。学完后必须能说明它的输入、状态所有权、执行顺序、不变量、失败传播和设计取舍。

## 2. 概念与项目语境

在“对象作用域、全局状态与生命周期”中，该节点位于“Session”分支。父专题中与它最接近的上下文如下：

HippoBuddy 至少存在四种生命周期：进程级 `ThreadPools/Config/MemoryStore`、Server 级 `DashboardServer`、Session 级 `ConversationComponents`、请求或任务级的 Tool 执行。对象 scope 必须与其持有资源的最短安全生命周期一致：把 SessionTranscript 做成全局对象会串写会话；每次请求创建线程池又会造成线程和队列泄漏。

`ConversationService.create()` 先构造 Conversation，再创建 warning、compact、memory、transcript 等配套组件，最后放入 registry，这个顺序避免正常调用通过 registry 观察到半初始化对象。但 `cleanupIdleSessions()` 只从 `componentRegistry/sessionLastAccessTime` 移除记录，没有像 `destroy()` 一样 flush/close transcript，也没有从 `conversationRegistry` 删除 Conversation；因此“从 Map 删除”不等于“生命周期正确结束”。

## 3. 底层实现原理

底层要明确输入、所有者、状态转移、输出和失败语义，并用单一边界维护相关不变量。

源码阅读固定拆成五步：输入如何规范化；谁拥有可变状态；状态按什么顺序转移；输出/事件如何闭合；并发、异常和重试后什么条件仍必须成立。对“TokenBudget”而言，任何只覆盖正常返回、没有解释失败状态的实现都不完整。

## 4. HippoBuddy 源码追踪

- [WorkspaceContext.java](../../../../../src/main/java/com/example/agent/desktop/WorkspaceContext.java)
- [WebSessionManager.java](../../../../../src/main/java/com/example/agent/web/session/WebSessionManager.java)
- [GracefulShutdown.java](../../../../../src/main/java/com/example/agent/core/concurrency/GracefulShutdown.java)
- [ThreadPools.java](../../../../../src/main/java/com/example/agent/core/concurrency/ThreadPools.java)
- [Config.java](../../../../../src/main/java/com/example/agent/config/Config.java)
- [MemoryStore.java](../../../../../src/main/java/com/example/agent/memory/MemoryStore.java)

项目原始实现片段：

```java
// WorkspaceContext：进程级可变状态
private static volatile String currentFolder;

public static void setCurrentFolder(String path) {
    String oldFolder = currentFolder;
    currentFolder = path;
    logger.info("工作区切换: {} -> {}", oldFolder, path);
}

// WebSessionManager：sessionId -> 会话资源
private static final Map<String, Conversation> sessions = new ConcurrentHashMap<>();
private static final Map<String, PendingToolCall> pendingToolCalls = new ConcurrentHashMap<>();
private static final Map<String, SessionTokenStats> sessionTokenStats = new ConcurrentHashMap<>();
```

从公开入口向下追踪到状态写入和外部适配器，再沿异常路径返回；记录“TokenBudget”在何处创建、验证、消费和释放。

## 5. 为什么要这样实现

单独实现它是为了隔离变化、支持失败恢复和独立测试。

更简单的写法通常把规则散落到调用者，短期代码少，长期会导致入口行为不一致、半初始化/半提交状态、无法恢复或难以替换测试依赖。

## 6. 完整代码实现

下面是父专题中已经通过 Java 21 编译检查的机制实现。围绕“TokenBudget”定位相关字段、分支和保护条件：

```java
import java.util.*;
import java.util.concurrent.*;
import java.util.concurrent.atomic.AtomicReference;

public class LifecycleDemo implements AutoCloseable {
    enum State { RUNNING, CLOSING, CLOSED }
    private final AtomicReference<State> state = new AtomicReference<>(State.RUNNING);
    private final Deque<AutoCloseable> resources = new ArrayDeque<>();
    private final ExecutorService executor = Executors.newFixedThreadPool(2);

    LifecycleDemo() { resources.push(this::stopExecutor); }

    synchronized <T extends AutoCloseable> T own(T resource) {
        if (state.get() != State.RUNNING) throw new RejectedExecutionException("closing");
        resources.push(resource);               // 后注册的依赖方先关闭
        return resource;
    }
    void submit(Runnable task) {
        if (state.get() != State.RUNNING) throw new RejectedExecutionException("closing");
        executor.submit(task);
    }
    private void stopExecutor() throws InterruptedException {
        executor.shutdown();
        if (!executor.awaitTermination(2, TimeUnit.SECONDS)) {
            executor.shutdownNow();
            if (!executor.awaitTermination(2, TimeUnit.SECONDS))
                throw new IllegalStateException("executor did not stop");
        }
    }
    @Override public void close() {
        if (!state.compareAndSet(State.RUNNING, State.CLOSING)) return; // 幂等
        RuntimeException failure = null;
        while (!resources.isEmpty()) {
            try { resources.pop().close(); }
            catch (Exception e) {
                if (failure == null) failure = new RuntimeException("close failed");
                failure.addSuppressed(e);       // 一个失败不能阻止后续资源关闭
            }
        }
        state.set(State.CLOSED);
        if (failure != null) throw failure;
    }

    public static void main(String[] args) {
        try (LifecycleDemo app = new LifecycleDemo()) {
            app.own(() -> System.out.println("session closed"));
            app.submit(() -> System.out.println("work"));
        }
    }
}
```

它用于证明机制；生产环境还要接入项目的认证、Session、取消、限流、日志、持久化和兼容策略。

## 7. 疑难点与失败推演

- 将该规则放错层，会让不同入口出现不同结果；
- 在对象完全初始化前发布，会让其他线程看到不完整状态；
- 先发送成功事件、后提交状态，崩溃后会造成不可恢复的假成功；
- Demo 中省略的生命周期、安全和耐久性保证不能直接带入生产。

## 8. 动手实验

1. 用 `rg "TokenBudget" src/main/java src/test/java` 搜索直接实现，再按本篇源码入口追踪间接实现；
2. 画出输入、拥有者、状态转移、输出和失败路径；
3. 运行完整实现，删除一个校验或调换状态更新顺序；
4. 写一个失败测试并说明被破坏的不变量；
5. 按“概念—源码—原理—为什么—缺口”口述三分钟。

## 9. 面试追问

**本质是什么？** 不只复述定义，要说清“TokenBudget”在 对象作用域、全局状态与生命周期 中控制的状态或协议责任。

**为什么不采用更简单写法？** 从隔离变化、维持不变量、失败恢复和可测试性回答。

**当前实现有什么不足？** 回到项目源码，区分当前事实、教学实现与生产目标。

## 10. 延伸学习

- [Java SE 21 Concurrency Guide](https://docs.oracle.com/en/java/javase/21/core/concurrency.html)：重点理解并发容器、锁与可见性并不能替代业务生命周期协议。
- [Java Language Specification §17](https://docs.oracle.com/javase/specs/jls/se21/html/jls-17.html)：精读 volatile 与 happens-before，解释 `currentFolder` 为什么“可见但非复合原子”。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段一：架构与对象模型
- **完整路线周次**：第 3/42 周
- **专题内进度**：第 7/12 个节点
- **全课程进度**：第 35/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[Conversation：从概念到底层源码](06.md)
- **下一知识点**：[Cancel Flag：从概念到底层源码](08.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

