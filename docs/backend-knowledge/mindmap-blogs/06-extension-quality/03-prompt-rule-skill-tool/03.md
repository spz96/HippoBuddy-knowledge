# mode：底层原理、源码与实现

> 所属专题：[Prompt、Rule、Skill、Tool 的边界](../../../06-extension-quality/03-prompt-rule-skill-tool.md)  
> 导图路径：Prompt → mode  
> 类型：Prompt/Rule/Skill/Tool 信任节点；编号 3/11

## 1. 学习结论

“mode”是Prompt/Rule/Skill/Tool 信任节点。掌握标准是能说明协议/状态、信任来源、生命周期、验证方法和失败恢复。

## 2. 概念与项目语境

“发布版本” Skill 描述检查 clean tree→测试→改版本→构建→tag；read_file/bash 是执行能力。Skill 不直接拥有 shell权限，Runtime仍根据 mode审批。这样流程知识可迭代，安全边界保持代码化。

**Skill 为什么不能自动获得执行权限？** Skill 是声明式知识和流程建议，不是 capability。即使 Skill 写着“执行 shell 并删除旧文件”，真正调用仍必须经过 Runtime 的 Tool Registry、AgentMode、PathSecurity 和确认策略。这样知识作者无法绕过权限边界。

## 3. 底层实现原理

组合器保留来源、trust、版本和稳定顺序；Prompt/Skill 影响模型，Rule/Capability 在执行端控制权限，Tool 才产生副作用。

源码阅读依次确认：对象或协议身份；状态所有者和迁移；跨线程/进程边界；权限与资源上限；异常、超时、重连或重启后的处理；测试与观测如何证明保证成立。

## 4. HippoBuddy 源码追踪

- [PromptService.java](../../../../../src/main/java/com/example/agent/prompt/PromptService.java)
- [RuleLoader.java](../../../../../src/main/java/com/example/agent/domain/rule/RuleLoader.java)
- [SkillLoader.java](../../../../../src/main/java/com/example/agent/domain/skill/SkillLoader.java)
- [Tool.java](../../../../../src/main/java/com/example/agent/llm/model/Tool.java)
- [AgentMode.java](../../../../../src/main/java/com/example/agent/core/AgentMode.java)

```java
public static List<SkillEntry> loadAllSkills(String workspacePath) {
    Map<String, SkillEntry> merged = new HashMap<>();
    for (SkillEntry e : loadUserSkills()) merged.put(e.getFileName(), e);
    for (SkillEntry e : loadProjectSkills(workspacePath)) merged.put(e.getFileName(), e);
    return merged.values().stream().sorted(...).toList();
}
```

围绕“mode”从注册/初始化追踪到执行、事件、清理和故障路径，区分项目当前能力与未来设计。

## 5. 为什么这样实现

这样不可信工作区或 Tool 输出不能通过文本指令提升硬能力。

扩展点若没有版本、权限、生命周期和契约测试，会把外部变化与不可信输入直接带入核心运行时。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.util.*;

public class PromptComposerDemo {
    enum Trust { SYSTEM, ADMIN_RULE, PROJECT_RULE, SKILL, UNTRUSTED_DATA }
    record Part(Trust trust, String id, int version, String content) {}
    static String compose(Collection<Part> input) {
        List<Part> parts = input.stream().sorted(Comparator
                .comparing(Part::trust).thenComparing(Part::id).thenComparingInt(Part::version)).toList();
        StringBuilder out = new StringBuilder();
        for (Part p : parts) {
            if (p.content().length() > 20_000) throw new IllegalArgumentException("part too large: " + p.id());
            out.append("<part trust=\"").append(p.trust()).append("\" id=\"")
                    .append(p.id().replace("\"", "")).append("\" version=\"")
                    .append(p.version()).append("\">\n")
                    .append(p.content()).append("\n</part>\n");
        }
        return out.toString();
    }
    static boolean canGrantCapability(Trust trust) {
        return trust == Trust.SYSTEM || trust == Trust.ADMIN_RULE;
    }
    public static void main(String[] args) {
        String p = compose(List.of(new Part(Trust.SKILL,"java",1,"workflow"),
                new Part(Trust.SYSTEM,"base",1,"policy")));
        if (p.indexOf("policy") > p.indexOf("workflow") || canGrantCapability(Trust.SKILL))
            throw new AssertionError(p);
    }
}
```

围绕“mode”注入依赖失败、超时、重连、恶意输入或并发终态，检查代码是否保持唯一且可观测的结果。

## 7. 疑难点与失败推演

- 线程池任务不等于隔离的 Agent 上下文；
- JSON-RPC 关联成功不代表远程 Tool 值得信任；
- Prompt 标签不能代替执行时 capability；
- 覆盖率、日志数量和 health=UP 都不能单独证明系统正确。

## 8. 实验与面试

1. 用 `rg "mode" src/main/java src/test/java` 追踪源码；
2. 画初始化、执行、故障和关闭时序；
3. 用 Fake、fixture、latch 或红队输入证明边界；
4. 按“概念—协议/状态—源码—为什么—验证与风险”回答。

## 9. 延伸学习

- [OWASP LLM Prompt Injection Prevention](https://cheatsheetseries.owasp.org/cheatsheets/LLM_Prompt_Injection_Prevention_Cheat_Sheet.html)：学习指令/数据分离、最小权限和输出监控。
- [OpenAI Prompt Engineering](https://platform.openai.com/docs/guides/prompt-engineering)：掌握稳定指令、结构化上下文和评测方法。
- [The Twelve-Factor App：Config](https://12factor.net/config)：借配置外置原则理解 Rule/Skill 与代码安全边界的分工。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段六：扩展、测试与威胁治理
- **完整路线周次**：第 38/42 周
- **专题内进度**：第 3/11 个节点
- **全课程进度**：第 469/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[behavior：底层原理、源码与实现](02.md)
- **下一知识点**：[constraints：底层原理、源码与实现](04.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

