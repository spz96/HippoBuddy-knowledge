# ERROR node：底层原理、源码与实现

> 所属专题：[Tree-sitter、WASM 与 Chicory](../../../06-extension-quality/05-tree-sitter-wasm.md)  
> 导图路径：Tree-sitter → ERROR node  
> 类型：JSON-RPC/MCP 协议节点；编号 3/12

## 1. 学习结论

“ERROR node”是JSON-RPC/MCP 协议节点。掌握标准是能说明协议/状态、信任来源、生命周期、验证方法和失败恢复。

## 2. 概念与项目语境

- [ ] 能解释 CST 和 ERROR node；
- [ ] 能说明 WASM 相对 JNI 的优势；
- [ ] 能区分语法与语义诊断；
- [ ] 能设计 parser 缓存和超时。

## 3. 底层实现原理

发送前登记 generation+id 对应 pending future，响应/超时/关闭 compare-remove；MCP 再协商版本和 capability 后暴露工具资源。

源码阅读依次确认：对象或协议身份；状态所有者和迁移；跨线程/进程边界；权限与资源上限；异常、超时、重连或重启后的处理；测试与观测如何证明保证成立。

## 4. HippoBuddy 源码追踪

- [TreeSitterWasmParser.java](../../../../../src/main/java/com/example/agent/domain/ast/TreeSitterWasmParser.java)

```java
synchronized (TreeSitterWasmParser.class) {
    int codePtr = (int) allocFn.apply(codeBytes.length)[0];
    int langPtr = (int) allocFn.apply(langBytes.length)[0];
    instance.memory().write(codePtr, codeBytes);
    long packed = parseFn.apply(codePtr, codeBytes.length,
                                langPtr, langBytes.length)[0];
    deallocFn.apply(resultPtr, resultLen);
}
```

围绕“ERROR node”从注册/初始化追踪到执行、事件、清理和故障路径，区分项目当前能力与未来设计。

## 5. 为什么这样实现

这样在乱序、超时和重连下仍正确关联请求，旧连接迟到结果不会污染新代际。

扩展点若没有版本、权限、生命周期和契约测试，会把外部变化与不可信输入直接带入核心运行时。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.nio.charset.StandardCharsets;
import java.util.*;

public class WasmAbiDemo {
    interface Guest {
        int alloc(int length); void write(int ptr, byte[] bytes);
        long parse(int sourcePtr, int sourceLength);     // high32=ptr, low32=len
        byte[] read(int ptr, int length); void free(int ptr, int length);
    }
    static String parseSafely(Guest guest, String source, int maxResultBytes) {
        byte[] input = source.getBytes(StandardCharsets.UTF_8);
        int inputPtr = guest.alloc(input.length);
        int resultPtr = 0, resultLen = 0;
        try {
            guest.write(inputPtr, input);
            long packed = guest.parse(inputPtr, input.length);
            resultPtr = (int)(packed >>> 32); resultLen = (int)packed;
            if (resultPtr <= 0 || resultLen < 0 || resultLen > maxResultBytes)
                throw new IllegalStateException("invalid guest result: " + resultPtr + "/" + resultLen);
            return new String(guest.read(resultPtr, resultLen), StandardCharsets.UTF_8);
        } finally {
            if (resultPtr > 0 && resultLen >= 0) guest.free(resultPtr, resultLen);
            guest.free(inputPtr, input.length);
        }
    }
}
```

围绕“ERROR node”注入依赖失败、超时、重连、恶意输入或并发终态，检查代码是否保持唯一且可观测的结果。

## 7. 疑难点与失败推演

- 线程池任务不等于隔离的 Agent 上下文；
- JSON-RPC 关联成功不代表远程 Tool 值得信任；
- Prompt 标签不能代替执行时 capability；
- 覆盖率、日志数量和 health=UP 都不能单独证明系统正确。

## 8. 实验与面试

1. 用 `rg "ERROR node" src/main/java src/test/java` 追踪源码；
2. 画初始化、执行、故障和关闭时序；
3. 用 Fake、fixture、latch 或红队输入证明边界；
4. 按“概念—协议/状态—源码—为什么—验证与风险”回答。

## 9. 延伸学习

- [Tree-sitter 官方文档](https://tree-sitter.github.io/tree-sitter/)：学习增量解析、错误恢复、grammar 和 syntax tree。
- [Chicory 源码与文档](https://github.com/dylibso/chicory)：理解纯 Java WASM runtime、Instance、Memory 和 Host functions。
- [WASI 官方站](https://wasi.dev/)：理解 capability-oriented 系统接口及 Host 授权边界。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段六：扩展、测试与威胁治理
- **完整路线周次**：第 40/42 周
- **专题内进度**：第 3/12 个节点
- **全课程进度**：第 494/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[parse tree：底层原理、源码与实现](02.md)
- **下一知识点**：[incremental parse：底层原理、源码与实现](04.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

