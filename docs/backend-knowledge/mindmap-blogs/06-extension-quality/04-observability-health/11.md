# subagent span：底层原理、源码与实现

> 所属专题：[日志、指标、Trace 与健康检查](../../../06-extension-quality/04-observability-health.md)  
> 导图路径：Traces → subagent span  
> 类型：可观测性与健康节点；编号 11/14

## 1. 学习结论

“subagent span”是可观测性与健康节点。掌握标准是能说明协议/状态、信任来源、生命周期、验证方法和失败恢复。

## 2. 概念与项目语境

TTFT、完整响应延迟、turn 数、Tool 成功率/耗时、confirmation 率、cache hit、input/output Token、任务成本、压缩次数、SubAgent queue wait、取消率、连续失败。

一个 chat request 为 root span；每轮 LLM、每个 Tool、SubAgent 为 child span；属性记录 provider/model/turn/toolName，不记录完整 Prompt/Key。SSE 长连接可记录首事件和完成事件。

## 3. 底层实现原理

日志、指标和 Trace 通过 trace/turn/tool id 关联；高基数字段留在日志/Span，health probe 并行、限时并区分存活与就绪。

源码阅读依次确认：对象或协议身份；状态所有者和迁移；跨线程/进程边界；权限与资源上限；异常、超时、重连或重启后的处理；测试与观测如何证明保证成立。

## 4. HippoBuddy 源码追踪

- [HealthCheckRegistry.java](../../../../../src/main/java/com/example/agent/core/health/HealthCheckRegistry.java)
- [LlmHealthIndicator.java](../../../../../src/main/java/com/example/agent/core/health/LlmHealthIndicator.java)
- [LoggingContext.java](../../../../../src/main/java/com/example/agent/core/logging/LoggingContext.java)
- [Tool.java](../../../../../src/main/java/com/example/agent/llm/model/Tool.java)
- [Prompt.java](../../../../../src/main/java/com/example/agent/prompt/model/Prompt.java)

```java
for (Map.Entry<String, HealthIndicator> entry : indicators.entrySet()) {
    try {
        Health health = entry.getValue().check();
        results.put(entry.getKey(), health);
        // DOWN > DEGRADED > UNKNOWN > UP
    } catch (Exception e) {
        results.put(entry.getKey(), Health.down().withException(e).build());
    }
}
```

围绕“subagent span”从注册/初始化追踪到执行、事件、清理和故障路径，区分项目当前能力与未来设计。

## 5. 为什么这样实现

这样能从症状追到单次 Agent 因果链，又不会让健康检查本身卡死或引发重启风暴。

扩展点若没有版本、权限、生命周期和契约测试，会把外部变化与不可信输入直接带入核心运行时。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.time.Duration;
import java.util.*;
import java.util.concurrent.*;
import java.util.function.Supplier;

public class HealthRegistryDemo implements AutoCloseable {
    enum Status { UP, DEGRADED, DOWN }
    record Health(Status status, String detail, long latencyMillis) {}
    private final ExecutorService executor = Executors.newVirtualThreadPerTaskExecutor();

    Map<String,Health> check(Map<String,Supplier<Health>> indicators, Duration timeout) {
        Map<String,CompletableFuture<Health>> futures = new LinkedHashMap<>();
        indicators.forEach((name, check) -> futures.put(name,
                CompletableFuture.supplyAsync(() -> {
                    long start = System.nanoTime();
                    try {
                        Health h = check.get();
                        return new Health(h.status(), h.detail(), (System.nanoTime()-start)/1_000_000);
                    } catch (Throwable e) {
                        return new Health(Status.DOWN, e.getClass().getSimpleName(),
                                (System.nanoTime()-start)/1_000_000);
                    }
                }, executor).completeOnTimeout(new Health(Status.DOWN,"timeout",timeout.toMillis()),
                        timeout.toMillis(), TimeUnit.MILLISECONDS)));
        Map<String,Health> out = new LinkedHashMap<>();
        futures.forEach((name,f) -> out.put(name, f.join()));
        return Map.copyOf(out);
    }
    public void close() { executor.shutdownNow(); }
}
```

围绕“subagent span”注入依赖失败、超时、重连、恶意输入或并发终态，检查代码是否保持唯一且可观测的结果。

## 7. 疑难点与失败推演

- 线程池任务不等于隔离的 Agent 上下文；
- JSON-RPC 关联成功不代表远程 Tool 值得信任；
- Prompt 标签不能代替执行时 capability；
- 覆盖率、日志数量和 health=UP 都不能单独证明系统正确。

## 8. 实验与面试

1. 用 `rg "subagent span" src/main/java src/test/java` 追踪源码；
2. 画初始化、执行、故障和关闭时序；
3. 用 Fake、fixture、latch 或红队输入证明边界；
4. 按“概念—协议/状态—源码—为什么—验证与风险”回答。

## 9. 延伸学习

- [OpenTelemetry：Signals](https://opentelemetry.io/docs/concepts/signals/)：系统掌握 traces、metrics、logs 和 baggage 的职责。
- [Google SRE Book（免费电子书）](https://sre.google/sre-book/table-of-contents/)：重点学习 SLI/SLO、监控分布式系统和错误预算。
- [Prometheus：Metric types](https://prometheus.io/docs/concepts/metric_types/)：理解 counter、gauge、histogram 与标签基数。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段六：扩展、测试与威胁治理
- **完整路线周次**：第 39/42 周
- **专题内进度**：第 11/14 个节点
- **全课程进度**：第 488/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[tool span：底层原理、源码与实现](10.md)
- **下一知识点**：[liveness：底层原理、源码与实现](12.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

