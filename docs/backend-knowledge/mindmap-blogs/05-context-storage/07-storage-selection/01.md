# zero service：底层原理、源码与实现

> 所属专题：[文件存储、SQLite 与 PostgreSQL 选型](../../../05-context-storage/07-storage-selection.md)  
> 导图路径：File → zero service  
> 类型：存储选型节点；编号 1/12

## 1. 学习结论

“zero service”是存储选型节点。掌握标准是能区分真相状态、派生索引/缓存、持久化确认点与失败恢复点。

## 2. 概念与项目语境

存储选型从访问模式和一致性需求出发：Transcript 是按 session 顺序追加与整段 replay，文件 JSONL 很自然；Memory 需要 metadata/filter/full-text，规模增长后 SQLite FTS 更自然；多实例协同任务需要并发事务、租约和唯一约束，PostgreSQL 更合适。数据量小不代表文件一定简单，跨对象事务和高并发 writer 才是分界。

HippoBuddy 当前 `WorkspaceManager/SessionStorage/SessionTranscript/MemoryStore` 以本地目录为主，优势是桌面应用可移植、可查看、无需服务进程；代价是多进程锁、查询、迁移、权限和 crash consistency 都由应用承担。不能把换 JDBC URL 称为迁移完成，领域层需先抽出按访问模式设计的 Repository/Log 端口。

## 3. 底层实现原理

按访问模式、一致性、writer 数和运维边界选择文件、SQLite、PostgreSQL 或对象存储，并通过 Repository 契约隔离实现。

源码阅读依次确认：数据单位和稳定身份；写入/更新顺序；内存可见与磁盘 durable 的边界；索引缓存失效；重放、压缩或迁移后的不变量。

## 4. HippoBuddy 源码追踪

- [WorkspaceManager.java](../../../../../src/main/java/com/example/agent/logging/WorkspaceManager.java)
- [SessionStorageFactory.java](../../../../../src/main/java/com/example/agent/session/SessionStorageFactory.java)
- [SessionStorage.java](../../../../../src/main/java/com/example/agent/session/SessionStorage.java)
- [SessionTranscript.java](../../../../../src/main/java/com/example/agent/session/SessionTranscript.java)

```java
Path sessionsDir = WorkspaceManager.getHippoRoot().resolve("sessions");
return new SessionStorage(sessionsDir, maxSavedSessions,
                          expireHours, tombstoneThresholdBytes);

public static Path getSessionMessagesFile(String sessionId) {
    return getSessionDir(sessionId).resolve("conversation.jsonl");
}
```

围绕“zero service”追踪数据从内存状态到日志/文件/索引，再从持久化记录恢复为运行时对象的双向路径。

## 5. 为什么这样实现

这样迁移不等于替换连接串，而能用 backfill、双读校验和切换窗口控制数据风险。

更简单的实现常混淆 estimator 与 Usage、flush 与 force、缓存与真相源，导致边界时超限、崩溃丢失或查询读到陈旧数据。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.sql.*;
import java.util.*;

public final class JdbcTranscriptRepository {
    private final Connection connection;
    public JdbcTranscriptRepository(Connection connection) throws SQLException {
        this.connection = connection;
        try (Statement s = connection.createStatement()) {
            s.executeUpdate("""
                create table if not exists transcript_event(
                  session_id varchar(128) not null,
                  seq bigint not null,
                  event_id varchar(36) not null unique,
                  event_type varchar(64) not null,
                  payload text not null,
                  primary key(session_id, seq)
                )
                """);
        }
    }
    void append(String session, long expectedSeq, UUID eventId, String type, String payload)
            throws SQLException {
        boolean auto = connection.getAutoCommit(); connection.setAutoCommit(false);
        try (PreparedStatement q = connection.prepareStatement(
                    "select coalesce(max(seq),0) from transcript_event where session_id=?")) {
            q.setString(1, session);
            try (ResultSet rs = q.executeQuery()) {
                rs.next(); long next = rs.getLong(1) + 1;
                if (next != expectedSeq) throw new SQLException("sequence conflict: expected " + next);
            }
            try (PreparedStatement i = connection.prepareStatement(
                    "insert into transcript_event(session_id,seq,event_id,event_type,payload) values(?,?,?,?,?)")) {
                i.setString(1, session); i.setLong(2, expectedSeq); i.setString(3, eventId.toString());
                i.setString(4, type); i.setString(5, payload); i.executeUpdate();
            }
            connection.commit();
        } catch (SQLException e) { connection.rollback(); throw e; }
        finally { connection.setAutoCommit(auto); }
    }
}
```

围绕“zero service”制造越级阈值、partial write、重复事件、外部编辑、索引损坏或检索 miss，验证恢复策略。

## 7. 疑难点与失败推演

- 字符数、估算 Token、服务端 Usage 和费用不是同一概念；
- JSONL 可追加不等于事务，atomic move 也不自动等于断电耐久；
- 摘要必须维持消息协议而不只是变短；
- index/cache 必须可从真相源重建，不能反过来成为唯一数据。

## 8. 实验与面试

1. 用 `rg "zero service" src/main/java src/test/java` 定位相关代码；
2. 画内存 → 队列 → 文件/数据库 → replay 的路径；
3. 在每个确认点前后模拟崩溃；
4. 按“数据模型—算法—源码—为什么—恢复与取舍”回答。

## 9. 延伸学习

- [SQLite Transactions](https://www.sqlite.org/lang_transaction.html)：理解本地单文件数据库的事务、锁与并发边界。
- [PostgreSQL Transaction Isolation](https://www.postgresql.org/docs/current/transaction-iso.html)：学习多 writer 下隔离级别和异常语义。
- [Designing Data-Intensive Applications](https://dataintensive.net/)：以访问模式、可靠性和运维成本进行存储选型，而不是只按数据量判断。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段五：上下文、持久化与检索
- **完整路线周次**：第 35/42 周
- **专题内进度**：第 1/12 个节点
- **全课程进度**：第 426/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[token budget：底层原理、源码与实现](../06-retrieval-progressive-disclosure/11.md)
- **下一知识点**：[readable：底层原理、源码与实现](02.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

