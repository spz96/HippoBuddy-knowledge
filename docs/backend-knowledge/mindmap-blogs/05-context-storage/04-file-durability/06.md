# sequence：底层原理、源码与实现

> 所属专题：[批量刷盘、幂等、原子写与崩溃恢复](../../../05-context-storage/04-file-durability.md)  
> 导图路径：Idempotency → sequence  
> 类型：JSONL 日志与重放节点；编号 6/12

## 1. 学习结论

“sequence”是JSONL 日志与重放节点。掌握标准是能区分真相状态、派生索引/缓存、持久化确认点与失败恢复点。

## 2. 概念与项目语境

UUID 防重复，不防 sequence 冲突；两条不同 UUID 的同一业务操作仍会重复。工具需要业务 callId。

项目 Transcript 的 `forceFlush()` 应在 session destroy/shutdown 前阻止新 append、排空队列、flush/force、再关闭 writer。单纯看到队列为空并不足够：消费者可能已经 take 但尚未落盘，需要一个 barrier/ack sequence 表明某个 seq 已 durable。

## 3. 底层实现原理

完整事件按单行 append，携带 eventId、sequence、type 与 schemaVersion；Loader 校验重复/缺口并从日志重建投影。

源码阅读依次确认：数据单位和稳定身份；写入/更新顺序；内存可见与磁盘 durable 的边界；索引缓存失效；重放、压缩或迁移后的不变量。

## 4. HippoBuddy 源码追踪

- [SessionTranscript.java](../../../../../src/main/java/com/example/agent/session/SessionTranscript.java)
- [SessionStorage.java](../../../../../src/main/java/com/example/agent/session/SessionStorage.java)
- [MemoryStore.java](../../../../../src/main/java/com/example/agent/memory/MemoryStore.java)

```java
// Transcript：批量写 + flush
writer.write(line);
writer.newLine();
writer.flush();

// Memory：临时文件 + force + 原子替换
channel.write(ByteBuffer.wrap(content.getBytes(UTF_8)));
channel.force(true);
Files.move(tempFile, file, ATOMIC_MOVE, REPLACE_EXISTING);
```

围绕“sequence”追踪数据从内存状态到日志/文件/索引，再从持久化记录恢复为运行时对象的双向路径。

## 5. 为什么这样实现

这样尾部损坏可隔离、历史可审计，但不能把 JSONL 格式本身误当事务或完整 Event Sourcing。

更简单的实现常混淆 estimator 与 Usage、flush 与 force、缓存与真相源，导致边界时超限、崩溃丢失或查询读到陈旧数据。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.io.IOException;
import java.nio.ByteBuffer;
import java.nio.channels.FileChannel;
import java.nio.file.*;

public final class DurableReplace {
    static void write(Path target, byte[] bytes) throws IOException {
        Path absolute = target.toAbsolutePath().normalize();
        Files.createDirectories(absolute.getParent());
        Path temp = Files.createTempFile(absolute.getParent(), ".durable-", ".tmp");
        boolean moved = false;
        try (FileChannel channel = FileChannel.open(temp, StandardOpenOption.WRITE,
                StandardOpenOption.TRUNCATE_EXISTING)) {
            ByteBuffer buffer = ByteBuffer.wrap(bytes);
            while (buffer.hasRemaining()) channel.write(buffer); // write 允许 partial
            channel.force(true);
        }
        try {
            Files.move(temp, absolute, StandardCopyOption.ATOMIC_MOVE,
                    StandardCopyOption.REPLACE_EXISTING);
            moved = true;
        } catch (AtomicMoveNotSupportedException e) {
            throw new IOException("atomic replace unsupported", e);
        } finally {
            if (!moved) Files.deleteIfExists(temp);
        }
    }
}
```

围绕“sequence”制造越级阈值、partial write、重复事件、外部编辑、索引损坏或检索 miss，验证恢复策略。

## 7. 疑难点与失败推演

- 字符数、估算 Token、服务端 Usage 和费用不是同一概念；
- JSONL 可追加不等于事务，atomic move 也不自动等于断电耐久；
- 摘要必须维持消息协议而不只是变短；
- index/cache 必须可从真相源重建，不能反过来成为唯一数据。

## 8. 实验与面试

1. 用 `rg "sequence" src/main/java src/test/java` 定位相关代码；
2. 画内存 → 队列 → 文件/数据库 → replay 的路径；
3. 在每个确认点前后模拟崩溃；
4. 按“数据模型—算法—源码—为什么—恢复与取舍”回答。

## 9. 延伸学习

- [Java FileChannel API](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/nio/channels/FileChannel.html)：精读部分写入、`force` 和并发语义。
- [PostgreSQL：Write-Ahead Logging](https://www.postgresql.org/docs/current/wal-intro.html)：借成熟数据库理解 WAL、提交与恢复。
- [Designing Data-Intensive Applications](https://dataintensive.net/)：系统学习存储引擎、日志、崩溃恢复和复制。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段五：上下文、持久化与检索
- **完整路线周次**：第 32/42 周
- **专题内进度**：第 6/12 个节点
- **全课程进度**：第 396/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[dedupe cache：底层原理、源码与实现](05.md)
- **下一知识点**：[temp sibling：底层原理、源码与实现](07.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

