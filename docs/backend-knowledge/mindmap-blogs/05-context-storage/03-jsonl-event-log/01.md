# one object per line：底层原理、源码与实现

> 所属专题：[JSONL、Append-only Log 与 Event Sourcing](../../../05-context-storage/03-jsonl-event-log.md)  
> 导图路径：JSONL → one object per line  
> 类型：JSONL 日志与重放节点；编号 1/12

## 1. 学习结论

“one object per line”是JSONL 日志与重放节点。掌握标准是能区分真相状态、派生索引/缓存、持久化确认点与失败恢复点。

## 2. 概念与项目语境

`append` 返回时只是成功放入内存队列，不代表已经写文件；队列满会直接跳过。Loader 解析失败会统计损坏行，但如果损坏发生在中间，继续加载后续行可能产生缺失投影。当前 UUID cache 还会过期且没有业务 operationId；不同 UUID 表示的同一工具副作用仍可重复。

JSONL 的一行边界就是一条记录的 framing，所以 payload 中真实换行必须由 JSON serializer 转义为 `\n`，writer 每次生成完整单行后再 append。它便于 tail、增量恢复和忽略最后一条 partial line，但 JSONL 本身不提供事务、fsync、唯一性或 schema evolution。

## 3. 底层实现原理

完整事件按单行 append，携带 eventId、sequence、type 与 schemaVersion；Loader 校验重复/缺口并从日志重建投影。

源码阅读依次确认：数据单位和稳定身份；写入/更新顺序；内存可见与磁盘 durable 的边界；索引缓存失效；重放、压缩或迁移后的不变量。

## 4. HippoBuddy 源码追踪

- [SessionTranscript.java](../../../../../src/main/java/com/example/agent/session/SessionTranscript.java)
- [TranscriptLoader.java](../../../../../src/main/java/com/example/agent/session/TranscriptLoader.java)
- [TranscriptEntry.java](../../../../../src/main/java/com/example/agent/session/TranscriptEntry.java)

```java
public void append(TranscriptEntry entry) {
    if (uuidCache.containsKey(entry.getUuid())) return;
    uuidCache.put(entry.getUuid(), System.currentTimeMillis());
    String jsonLine = objectMapper.writeValueAsString(entry);
    if (!writeQueue.offer(jsonLine, 100, TimeUnit.MILLISECONDS)) {
        uuidCache.remove(entry.getUuid());
    }
}
```

围绕“one object per line”追踪数据从内存状态到日志/文件/索引，再从持久化记录恢复为运行时对象的双向路径。

## 5. 为什么这样实现

这样尾部损坏可隔离、历史可审计，但不能把 JSONL 格式本身误当事务或完整 Event Sourcing。

更简单的实现常混淆 estimator 与 Usage、flush 与 force、缓存与真相源，导致边界时超限、崩溃丢失或查询读到陈旧数据。

## 6. 完整代码实现

以下机制实现已通过 Java 21 编译检查：

```java
import java.io.*;
import java.nio.charset.StandardCharsets;
import java.nio.file.*;
import java.util.*;

public final class JsonlLogDemo {
    record Event(UUID id, long seq, int version, String type, String payload) {}
    static String encode(Event e) {
        String escaped = e.payload().replace("\\", "\\\\").replace("\"", "\\\"")
                .replace("\r", "\\r").replace("\n", "\\n");
        return e.id() + "\t" + e.seq() + "\t" + e.version() + "\t" + e.type() + "\t" + escaped;
    }
    static void append(Path file, Event event) throws IOException {
        String line = encode(event) + "\n";
        Files.writeString(file, line, StandardCharsets.UTF_8,
                StandardOpenOption.CREATE, StandardOpenOption.APPEND);
    }
    static List<Event> replay(Path file) throws IOException {
        List<Event> out = new ArrayList<>(); Set<UUID> seen = new HashSet<>(); long expected = 1;
        for (String line : Files.readAllLines(file, StandardCharsets.UTF_8)) {
            if (line.isBlank()) continue;
            String[] p = line.split("\t", 5);
            if (p.length < 5) break;                     // 只容忍文件尾 partial record
            Event e = new Event(UUID.fromString(p[0]), Long.parseLong(p[1]),
                    Integer.parseInt(p[2]), p[3], p[4]);
            if (!seen.add(e.id())) continue;
            if (e.seq() != expected++) throw new IOException("sequence gap at " + e.seq());
            out.add(e);
        }
        return List.copyOf(out);
    }
}
```

围绕“one object per line”制造越级阈值、partial write、重复事件、外部编辑、索引损坏或检索 miss，验证恢复策略。

## 7. 疑难点与失败推演

- 字符数、估算 Token、服务端 Usage 和费用不是同一概念；
- JSONL 可追加不等于事务，atomic move 也不自动等于断电耐久；
- 摘要必须维持消息协议而不只是变短；
- index/cache 必须可从真相源重建，不能反过来成为唯一数据。

## 8. 实验与面试

1. 用 `rg "one object per line" src/main/java src/test/java` 定位相关代码；
2. 画内存 → 队列 → 文件/数据库 → replay 的路径；
3. 在每个确认点前后模拟崩溃；
4. 按“数据模型—算法—源码—为什么—恢复与取舍”回答。

## 9. 延伸学习

- [JSON Lines 规范](https://jsonlines.org/)：掌握 UTF-8、每行一个 JSON value 和换行约定。
- [Martin Fowler：Event Sourcing](https://martinfowler.com/eaaDev/EventSourcing.html)：理解事件日志、重放、快照和逆向事件。
- [Designing Data-Intensive Applications](https://dataintensive.net/)：深入日志、复制、批处理与流处理的共同模型。

---

返回[节点博客目录](README.md)或[后端知识库总索引](../../../README.md)。

## 系统学习位置与验收

- **课程阶段**：阶段五：上下文、持久化与检索
- **完整路线周次**：第 31/42 周
- **专题内进度**：第 1/12 个节点
- **全课程进度**：第 379/538 个节点
- **建议投入**：首学 25–40 分钟，源码/实验 30–45 分钟，间隔复习每次 10–15 分钟
- **上一知识点**：[failure count：底层原理、源码与实现](../02-context-compaction/13.md)
- **下一知识点**：[streamable：底层原理、源码与实现](02.md)

完成本节点前，不以“读过”为标准，而要留下以下证据：

- [ ] 闭卷写出概念、边界和底层机制；
- [ ] 在 HippoBuddy 中定位入口类、状态拥有者和失败出口；
- [ ] 运行完整代码，并解释每个关键字段/分支；
- [ ] 删除一个保护或注入一个失败，说明破坏的不变量；
- [ ] 完成三分钟面试口述，能回答至少两层追问。

间隔复习记录：

- [ ] D1：闭卷定义与源码类；
- [ ] D3：重画流程/状态机；
- [ ] D7：复现失败实验；
- [ ] D14：回答面试追问；
- [ ] D30：换一个入口重新追踪源码。

课程工具：[系统化学习指南](../../../SYSTEMATIC_LEARNING_GUIDE.md) · [全量进度表](../../../MASTER_PROGRESS_TRACKER.md)

